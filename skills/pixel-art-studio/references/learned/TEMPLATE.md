# Study: <ชื่องาน/สไตล์>

- **source**: <path หรือที่มา> · studied: <YYYY-MM-DD>
- **true size**: WxH (detected scale Nx) · **colors**: N · **palette saved as**: `<name>` / not saved
- **subject**: <what the art depicts>

## Measurements (from report.json)
- ramps: <hue buckets → steps each; hue-shift direction/strength observed>
- outline: <none / selout / black — edge_dark_pct, edge colors>
- dithering: <checker % + WHERE it's actually used (from looking, not just the number)>
- value range: <min–max; high/low contrast style?>

## Observations (from LOOKING at zoom.png — pixel-level, specific)
- <e.g. "shadow band is 2–3px, cuts diagonally across the torso, never parallel to outline">
- <e.g. "AA only on the head curve, single px, uses ramp step 2">
- <e.g. "eyes are 2x2 dark + 1px white glint top-left, no whites">
- <cluster sizes, silhouette tricks, light direction, specular treatment>

## Rules to apply (imperative — used when creating in this style)
- <"use 4-step ramps, hue_shift≈18, shadows toward purple">
- <"outline: selout darkest shade, break on lit top edge">
- <"...">

## Do NOT copy
- <weaknesses, quirks that don't generalize, things that fail the checklist>
