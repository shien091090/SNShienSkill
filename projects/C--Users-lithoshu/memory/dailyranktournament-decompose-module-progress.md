---
name: dailyranktournament-decompose-module-progress
description: 分組日榜(DailyRankTournament) activity-decompose-module 進度，29個模塊中28個已完成，剩DRT_FirstOpenTrigger待處理；2026-08-13已完成一輪既有模塊的行為調整迭代（詳見文末2026-08-13快照）
metadata: 
  node_type: memory
  type: project
  originSessionId: 176f2d81-e077-4c2f-9332-ac215d7b56de
  modified: 2026-08-14T08:32:59.098Z
---

分組日榜活動系統（Slot_Lobby 專案）正在跑 `activity-decompose-module`（拆解模塊）skill，與 [[dailyranktournament-match-view-progress]] 的 View/prefab 進度是不同軌道（那個是 parse-ui-mockup 賽事頁 View，這個是全系統 Model 模塊拆解）。

**目前進度：**
1. Step 5、Step 6 已完成：23 個模塊空殼、架構設計文件都已建立（`Slot_Lobby\docs\開發文件與示意圖\活動系統\分組日榜(DailyRankTournament)\程式架構設計\DailyRankTournament_程式架構設計.md`）
2. 「是否為待處理」欄位單純判斷 class 本體是否仍為空殼

**⚠️ 命名鐵律：「資格」一律用 `Qualification`，不是 `Eligibility`**（Server API 層從一開始就用這個詞，詳見 `DRT_MemberQualificationRequest`/`Response`）

**📄 Server API 文件位置**：`Slot_Lobby\docs\開發文件與示意圖\活動系統\分組日榜(DailyRankTournament)\Server文件\_分組日榜 DailyRankTournament.xlsx`，欄位定義在 `APIDatastructure` 分頁、API 清單在 `API` 分頁。這是 .xlsx 檔，Read 工具無法直接讀取，要用 Python 讀（`zipfile` 解壓後解析 `xl/worksheets/sheetN.xml` + `xl/sharedStrings.xml`，或先確認環境是否已裝 `openpyxl`）。2026-07-20 使用者提到這份文件之前沒更新到最新版，已用新版覆蓋，並請我逐一比對 `DRT_MemberQualificationResponse`/`DRT_TournamentInfoResponse`/`DRT_RewardInfoResponse` 目前程式碼跟文件是否一致——結果：`qualificationOngoing` 註解需要補完整定義、`dailyMatchWindowEnd` 欄位名稱依文件改成 `dailyMatchWindow`（其餘欄位跟註解都已一致）。之後每次要新串接 Server API 欄位，都應該先看這份文件的 `APIDatastructure` 分頁做核對，不要憑空猜欄位名稱。

**已完成 2/23 個模塊：**

1. **`DRT_QualificationTierSettings`**（明日賽事資格設定）— Step 7~10 全跑完，測試通過
2. **`DRT_PlayerQualificationManager`**（玩家明日賽事資格管理器，2026-07-16 完成）— 24 個測試全數通過
   - 依賴 `DRT_QualificationTierSettings` 取得階級門檻，自行用「今日押碼量 vs 門檻」算出 `DRT_QualificationStatus`（currentLevel/nextLevel/progressValue/progressGoal/progressPercentage），**不是**直接讀 Server API 的 `DRT_MemberQualificationResponse.currentLevel` 等現成欄位（該 response 雖然有這些欄位，但 client 端要能靠即時 WS 推播的押碼量自己重算，所以獨立實作了一套判斷邏輯）
   - `DRT_QualificationStatus`（`DataStructures/Model/`）：`currentLevel`(int)、`nextLevel`(int)、`progressValue`(long)、`progressGoal`(long)、`progressPercentage`(**float, 0~1**，四捨五入到整數百分比後再除以100，例如 33.33...% → 0.33f)
   - 押碼量範圍演算法：中間階級 = `[本階門檻, 下一階門檻)`；最後一階 = `[上一階門檻, 本階門檻]`（用上一階門檻當分母基準）；尚未達第一階 = `[0, 第一階門檻)`
   - WS 事件 `ExtensionResponse.DRT_MemberBetUpdate = "DRT_MemberBetUpdate"`：**這是通靈值，Server 尚未定義實際格式**，之後 Server 文件確定後要回來改成真正的字串；資料結構假設只有 `todayBet`（`DRT_MemberBetUpdateResponse`），若之後 Server 決定推更多欄位需要回來調整
   - 新增 3 個 `DailyRankTournamentSubEventId`：`MemberQualificationResponse`、`MemberBetUpdate`、`MemberBetAndLevelUpdate`
   - `IsMaxLevel()` 後續改為直接讀 `qualificationResponse.isMaxLevel`（Server 已算好），不再自己比對階級門檻
   - **⚠️ 2026-07-17 修正過的架構誤植**：`DRT_QualificationTierSettings.GetQualificationTimeInfo`/`GetTodayQualificationTimeInfo`（資格賽起訖時間）與 `DRT_PlayerQualificationManager.GetMatchStartTime`（比賽開始時間）已移除，因為賽事時間該歸 `DRT_MatchTimeManager` 管、資格賽時間該歸 `DRT_QualificationTimeManager` 管（兩者目前仍是空殼待處理）。誤植原因：使用者換手時把這些案例列在這兩個 class 的 test 檔案裡，當下照著實作了，沒有先核對架構文件的模塊職責定義。連同 `DRT_QualificationTierSettings.Initialize` 的 `ITimeManager` 依賴、相關 4+2 個 unit test 案例、`CreateTournamentInfoResponse`/`CreateQualificationResponse` 工廠方法裡對應的多餘參數都一併移除，等實際實作到那兩個模塊時再補上。這類錯誤已促成 `activity-iterate-module` 新增「實作前檢查」流程，見下方
   - **⚠️ 修正過的錯誤**：一開始 `Initialize()` 誤傳了具體類別 `DRT_QualificationTierSettings` 而非 interface，後來補建介面（只含 query 方法，不含 `Initialize` 本身）並改用該介面；測試也同步從「建構真實 `DRT_QualificationTierSettings` 實例 + 模擬其內部事件」大幅簡化成直接 `Substitute.For<介面>()` mock
   - **⚠️ 介面命名鐵律**：有系統前綴的類別（`DRT_XxxYyy`），介面的 `I` 要插在前綴**之後**：`DRT_QualificationTierSettings` → `DRT_IQualificationTierSettings`，不是 `IDRT_QualificationTierSettings`（一開始建錯過，已改名）
   - 上面這兩條規則（Initialize 參數必須 interface、介面命名規則）已經**從 skill 抽出來**，搬到 aiToolLib 新增的通用規則 `rules/21-mvp-model-architecture.md`（連同 `CreateModuleInstances`/`InitializeModules` 兩階段模式），2026-07-17 建立。這份規則**只適用於 MVP 架構**（Model 拆多個子模塊互相依賴，如 Guild/BattlePass/DailyRankTournament），舊功能或非 MVP 架構的 Model 不適用，不用為了套規則硬改架構

3. **`DRT_MatchSettings`**（賽事設定，2026-07-17 完成）
   - **⚠️ 架構調整**：獎勵相關流程（`GetLevelRewards`/`GetLevelRankReward`，讀 `DRT_RewardInfoResponse.levelRewards`）原本實作在 `DRT_QualificationTierSettings`，後來確認「獎勵屬於賽事職能」而整批移到這個模塊；`DRT_QualificationTierSettings` 現在只負責階級門檻/中文名稱，不再監聽 `RewardInfoResponse`
   - `DRT_RewardInfoResponse.seasonRewards`（`List<DRT_RankReward>`，不分階級的純名次獎勵）留給未來 `DRT_SeasonSettings` 使用，跟這次移動無關
   - 刷新頻率（`GetLeaderboardRefreshInterval`）、顯示名次上限（`GetDisplayRankCount`）**不是 Server 資料，改用 ScriptableObject**（`DRT_MatchLeaderboardScriptableObject` 實作 `DRT_IMatchLeaderboardSetting`，仿照 `BattlePassModel.newActivityHintSetting` 的模式：`UIPrefabsDefine` 存路徑常數、`AssetsManager.Instance.LoadAsset<T>()` 在 `CreateModuleInstances()` 載入、`Initialize()` 注入 interface），資產在 `Assets/SourceFiles/DailyRankTournament/ScriptableObject/MatchLeaderboardSetting.asset`
   - 這是本次迭代第一個確認「Server 沒有欄位、改走 SO 設定」的案例，之後若其他模塊也查不到對應 Server 欄位，可以先問使用者是否也是 SO 模式

4. **`DRT_QualificationTimeManager`**（資格賽時間管理器，2026-07-20 完成）
   - 監聽 `TournamentInfoResponse`（資格賽起訖時間字串來源）與 `MemberQualificationResponse`（`qualificationEndTime` 為資格結算倒數目標時間；**「當天是否有資格賽」直接讀 `qualificationOngoing` 欄位**，不是用 `qualificationEndTime>0` 推斷——這是 2026-07-20 事後修正過的，一開始誤用 `qualificationEndTime` 當判斷依據，Server 後來確認有獨立的 `qualificationOngoing` 欄位）
   - `GetTodayQualificationTimeInfo()` 的 endTime 優先讀 `qualificationResponse.qualificationEndTime`，**拿不到 MemberQualificationResponse 時 fallback 用 TournamentInfoResponse 推算**；但若 `qualificationResponse` 存在且 `qualificationOngoing==false`（明確「當天沒有資格賽」）→ 直接回傳 null，不 fallback
   - 倒數計時用既有的 `ITimeManager.SetCountDownListener(seconds, callback, key)` 模式（跟 BattlePassDailyLoginHandler 一致），倒數完畢先發送 `QualificationEndTimeReached` 事件，再視「資格頁面是否開啟」決定要不要延遲後發送 `RequestMemberQualification` 事件
   - **⚠️ 新增共用模塊 `RandomDelayProvider`**（`Assets/Scripts/Lobby/Models/LobbyActivityCommon/RandomDelayProvider/`，`IRandomDelayProvider.DelayRandomSeconds(min,max)` 包裝 `UniTask.Delay` + `Random.Range`）：因為使用者要求「隨機1~10秒的延遲」要能被 mock（不要讓消費端的單元測試真的等隨機秒數），且這個功能夠通用值得抽成共用模塊，已補進 `rules/19-common-modules.md`。它自己的測試（`RandomDelayProviderTest`）是少數會**真的等待**的測試（用小秒數如0~2秒 + Stopwatch量測），因為它是最底層、沒有更下層可以 mock 的葉節點；所有消費端（如 `DRT_QualificationTimeManager`）都直接 mock `IRandomDelayProvider`，用 `.Returns(UniTask.CompletedTask)` 讓 await 同步完成，不需要真的等待
   - **⚠️ 依賴尚未實作的模塊**：`DRT_IQualificationPageViewPresenter`（`IsQualificationPageOpen()`）介面已建立，但 `DRT_QualificationPageViewPresenter` 本身還是空殼，`DailyRankTournamentModel` 目前注入 `null`（配合 production code 的 `?.` null-safe 呼叫），等之後真正實作到該 Presenter 時再補上實例注入。這是「先建 interface 佔位、之後才補實作」模式的第一個案例
   - **⚠️ 架構文件筆誤修正**：原本 `DRT_QualificationTimeManager` 的用途概述誤寫了「開始競賽時間/開始賽事事件」，那其實是 `DRT_MatchTimeManager` 的職責，已在架構文件訂正

5. **`DRT_ApiRequestHandler`**（API請求器，2026-07-20 完成）——**這是使用者中途追加、不在原始23個模塊清單裡的新模塊**，透過「迭代模塊」流程新增
   - 監聽通用「請求API」`RequestApi` SubEventId（帶 `DRT_ApiRequestParams` 參數，內含 `List<DRT_ApiType> apiTypes`），依序（**SEQUENTIAL**，不是平行）執行請求，可設定 `abortOnFailure`（失敗中斷後續）、`sendCompletionEventImmediately`（每個成功當下發送 vs 全部完成才批次發送）、`showLoading`、`callback`（全部完成後執行，不論成功失敗）
   - 內部用 `Dictionary<DRT_ApiType, ApiRequestAction>` 登記表設計（`RegisterApiRequest`/`UnregisterApiRequest` 皆為 public），**7 個真實 API 都已註冊**（TournamentInfo/MemberStatus/MemberQualification/MemberMatch/MemberMatchInfo/SeasonLeaderboard/RewardInfo），成功時發送對應的既有 SubEventId（如 `MemberQualificationResponse`）並帶上真實 response 物件；新增了 `MemberStatusResponse`/`MemberMatchResponse`/`MemberMatchInfoResponse`/`SeasonLeaderboardResponse` 這4個之前沒有的 SubEventId
   - error handle 統一走 `ILobbyActivityServerCommunicator.ParseErrorCode(statusCode, isRestartWhenFailed: false)`
   - **⚠️ 意外牽出的相依鏈**：Request DTO 需要 `activityId`，專案既有做法是透過 `ActivityPeriodManager`（`IActivityPeriod.CurrentActivityId`），這需要 `ActivityCode.DailyRankTournamentExternalActivityName`（**已經有人事先登記好 `A042`**，不用自己加）+ `ActivityModel.Instance` + `BaseDataManager.Instance.GetLobbyData<IActivityInfoRecordData>()`；`memberId` 則用 `UserDataManager.profile.memberInfo.memberID`；Loading 畫面用 `ILoadingIndicator`（`LoadingIndicatorModel.Instance`，`Show(this)`/`Close(this)` 帶 key 版本，確保多個並發流程不會互相打架）
   - 單元測試設計採**雙軌**：register/error-handle/registered-lookup 這幾個測試用「真的」`DRT_ApiType.MemberQualification` + mock `serverCommunicator.SendServerPostRequest<T>`；串聯邏輯（abort/immediate-vs-batch/callback/loading）改用 `RegisterApiRequest` 動態覆蓋掛假的 action，不用真的碰 Server DTO，其中 immediate-vs-batch 是靠捕捉「呼叫順序」（`List<string> callOrder`）才能真正驗證出行為差異，只驗證「最後有沒有發送」是驗證不出來的假陽性

6. **`DRT_ActivityInitializer`**（活動初始化器，2026-07-20 完成）
7. **`DRT_ActivityTimeUpdater`**（活動時間更新器，2026-07-20 完成，**已於 2026-07-21 被移除**——職責整併進新建的 `DRT_ActivityPeriodManager`，見下方第9項）
8. **`DRT_DefaultTabManager`**（預設頁籤管理器，2026-07-21 完成）——依賴 `DRT_ISeasonSettings`/`DRT_IMatchStateManager`（介面已建立，兩個實作都還是空殼，目前注入 null）
9. **`DRT_ActivityPeriodManager`**（活動期間管理器，2026-07-21完成）——取代共用模塊 `ActivityPeriodManager`，並整併原 `DRT_ActivityTimeUpdater` 的計時器與活動狀態事件發送職責；當初「未commit破壞編譯異動」（欄位被註解但用法沒清）已於這次實作中一併修正完畢，目前 `DailyRankTournamentModel.cs` 可正常編譯
10. **`DRT_MatchTimeManager`**（賽事時間管理器，2026-07-21完成）——`GetCurrentMatchTimeInfo`/`GetMatchStartTimeString`/`GetMatchEndTimeString` 與倒數計時器；`IRandomDelayProvider` 新增可選 `CancellationToken` 參數支援中途取消；今日日期計算改用 `GetCurrentLocalTime`（避免清晨時段誤算成前一天）
11. **`DRT_MatchStateManager`**（賽事狀態管理器，2026-07-21完成）——`HasOngoingMatch`/`GetCurrentMatchId`/`GetCurrentMatchLevel`/`IsMatchStateExpired`
12. **`DRT_DailyRankTournamentViewPresenter`**（分組日榜主頁面呈現器，2026-07-21~22完成）——打開活動頁面/切換頁籤/活動關閉流程，含 `BindView`/`UnbindView`，新增 `DRT_IDefaultTabManager`/`DRT_IMatchPageViewPresenter`/`DRT_ISeasonPageViewPresenter`/`DRT_IDailyRankTournamentView` 介面；View 層已補上 BindPresenter 雙向綁定，並已跑過 `parse-ui-mockup` 掛 prefab（2026-07-22 最新commit：賽季頁籤小紅點顯示/隱藏接口 + 賽季頁TopAward獎勵顯示流程 + prefab掛腳本，屬 View 層工作，不算入模塊拆解進度）

**2026-07-22 現況更新（架構文件+程式碼實查，非單純記憶推測）：** 目前 25 個模塊中 **11 個已完成**（上述 1~5、6、8~12，`DRT_ActivityTimeUpdater` 已被移除不計入25個模塊清單），**14 個待處理**，依架構文件列表順序，下一個要處理的是 **`DRT_MatchRankingManager`**（賽事排行榜管理器）——目前 `DRT_MatchRankingManager.cs` 與其測試檔 `DRT_MatchRankingManagerTest.cs` 都還是純空殼（無任何測試案例註解），代表 Loop 還沒真的進到這個模塊的實作階段，需要先請使用者「換手」（在 test 檔案用註解列出測試案例）。

**2026-07-23 完成 `DRT_MatchLeaderboardAutoRefresher`**（commit `0e6b960d7a0`）：18 個測試全過，串接進 `DailyRankTournamentModel`（依賴 `IYileEventAdapter`/`IYileEventTools`/`ITimeManager`/`DRT_IMatchStateManager`/`DRT_IMatchTimeManager`/`DRT_IMatchLeaderboardSetting`，純事件驅動無對外查詢方法）。
- **⚠️ 換手案例曾有筆誤已修正**：「距離賽事結束時間大於倒數計時時間時,則不啟動」應為「小於」——判斷公式統一為 `gap = 賽事結束時間 - 倒數計時結束時間點`；`gap<0`（剩餘時間 < 倒數計時時長）與 `0<=gap<=10秒` 都不啟動，`gap>10秒` 才啟動
- 倒數計時時間固定從 `DRT_IMatchLeaderboardSetting.GetLeaderboardRefreshSeconds()` 拿，不是 `DRT_MatchSettings.GetLeaderboardRefreshInterval()`（後者只是包裝前者給外部查詢用）
- 現在 25 個模塊中 **15 個已完成**，12 個待處理，依架構文件列表順序下一個是 **`DRT_MatchLeaderboardDropdownParser`**（賽事榜單下拉式選單解析器），目前仍是純空殼，尚未換手

**2026-07-23 完成 `DRT_MatchRankingManager`**（commit `e5f4469a396`）：14 個測試全過，串接進 `DailyRankTournamentModel`（依賴 `IYileEventAdapter`/`IYileEventTools`/`ITimeManager`，無其他模塊依賴，未建 interface）。用途概述已改寫為「儲存玩家歷史賽事摘要清單與當前進行中賽事排行榜資料，並提供指定賽事的歷史榜單查詢」（拿掉15分鐘定時描述）。現在 25 個模塊中 **14 個已完成**，13 個待處理，依架構文件列表順序下一個是 **`DRT_MatchLeaderboardAutoRefresher`**——其測試檔已有使用者事先寫好但**尚未commit**的換手案例（4 個案例，圍繞頁籤開啟/賽事進行中/倒數計時器啟動條件），可以直接跳過等待換手、進入實作前檢查。

**2026-07-23 恢復工作時再次核實（比上面這則更新）：**
- 實際已完成 13 個（上述 11 個 + `DRT_RedPointHandler` + `DRT_QualificationPageViewPresenter`，這兩個是後來以「迭代模塊」單獨觸發方式完成，不是照架構文件列表順序處理，但不影響下一個待處理判斷）
- `DRT_MatchRankingManager` 換手已完成且已 commit（`72e7452272f [test] [分組日榜] [賽事排行榜管理器] 列TestCase`），但 production class 本體仍是空殼——**Loop 卡在「換手完成、實作前檢查尚未做」的階段**，需要先跟使用者過一遍 `activity-iterate-module` 的「實作前檢查」三項（架構符合度／Server API串接／案例合併），確認後才能實作
- 工作區另有**未 commit** 的 `DRT_MatchLeaderboardAutoRefresherTest.cs`（清單中下一個待處理模塊）測試案例註解，已經先寫好但還沒進入該輪迭代——這是使用者提前補的換手素材，等 `DRT_MatchRankingManager` 完整跑完一輪（實作→ViewPresenter檢查→串接進Model→更新架構文件）後，下一個模塊可以直接跳過「等待換手」直接做實作前檢查
- Server API 資料結構已齊全，不需要再建立：`DRT_MemberMatchInfoResponse.matchInfos`(List<DRT_MatchInfo>) 對應「歷史榜單」、`DRT_MemberMatchResponse`（`leaderboard`/`matchStartTime`/`matchEndTime`）對應「當前進行中榜單」，`DRT_ApiRequestParams.matchId` 已支援指定賽事查詢，`DRT_ApiType.MemberMatch`/`MemberMatchInfo` 與對應 SubEventId 都已在 `DRT_ApiRequestHandler` 註冊好
- 架構文件裡 `DRT_MatchRankingManager` 的「用途概述」提到「每15分鐘一次打API更新榜單」，但這個定時刷新職責實際上已經分給 `DRT_MatchLeaderboardAutoRefresher`（其換手測試案例也完全沒有計時器邏輯），**這段用途概述需要跟使用者確認後改寫**，不要直接覆蓋

**2026-08-11 追加：`DRT_MatchTimeManager` 改用賽事狀態(`DRT_IMatchStateManager.GetMatchStatus()`)取代時間區間比對**（commit `fdc1da7185d`）：
- `GetCurrentMatchTimeInfo()`原本用`currentTime < matchStartTime || currentTime > matchEndTime`判斷是否在賽事區間內，改成直接讀`matchStateManager.GetMatchStatus()`，非`Waiting`/`Ongoing`才回null——這個方法內因此完全不再用到`timeManager.GetCurrentTime`（該欄位在class裡其他方法仍有用到，沒有變成無用）
- `OnMatchStateUpdateComplete()`原本只看`HasOngoingMatch()`二選一啟動"賽事開始"或"賽事結束"倒數計時器；改成`HasOngoingMatch() && GetMatchStatus()==Ongoing`才啟動"賽事結束"，其餘情況(無賽事、或有賽事但狀態為`Waiting`配對中)一律啟動"賽事開始"倒數計時器——新增1個測試案例(配對中→賽事開始倒數，3組跨日TestCase跟"無賽事"那組共用相同數值，因為都是同樣的`dailyMatchWindowStart`目標)，既有"有賽事→賽事結束倒數"測試補上`GivenMatchStatus(DRT_MatchStatus.Ongoing)`並改名為`..._and_ongoing_then_start_match_end_count_down`
- **⚠️ 共用測試helper連動遺漏點**：`GivenMatchEndCountDownArmed()`這個被4個"賽事結束倒數計時器時間到"系列測試(`match_end_count_down_time_out_*`)共用的helper，只設了`HasOngoingMatch(true)`沒設matchStatus——NSubstitute對enum預設回傳0(`DRT_MatchStatus.None`)，新production邏輯下會導致這個helper實際武裝到的是"賽事開始"倒數計時器而非"賽事結束"，讓那4個測試的`countDownCallback`指向錯的方法卻可能因為兩者都呼叫同一個`OnMatchCountDownTimeOut()`私有方法而表面上仍然"通過"（測試斷言的是事件是否送出，不是倒數計時器key本身）——已補上`GivenMatchStatus(DRT_MatchStatus.Ongoing)`。**這是本次修改「任何一個判斷分支從單一bool改成多狀態」時的通用提醒**：要地毯式找出所有共用的`Given...Armed()`類型helper，逐一確認它們預期武裝的是哪個分支，不能只看新增/修改的那幾個測試案例本身。
- 662個DailyRankTournament測試全過(658→662)。：2026-07-21 曾記錄過 `DailyRankTournamentModel.cs` 的 `activityPeriodManager` 欄位被註解掉但用法沒清、無法編譯的狀態，該問題已在後續 `DRT_ActivityPeriodManager` 實作 commit 中一併修正，目前該檔案編譯正常，不需要再處理。

**其餘 14 個模塊維持原樣待處理**（`DRT_MatchRankingManager`（下一個）、`DRT_SeasonSettings`、`DRT_SeasonTimeManager`、`DRT_SeasonStateManager`、`DRT_SeasonRankingManager`、`DRT_FirstOpenTrigger`、`DRT_ToastPopupManager`、`DRT_ToastPopupQueueHandler`、`DRT_RedPointHandler`、`DRT_QualificationPageViewPresenter`、`DRT_MatchPageViewPresenter`、`DRT_SeasonPageViewPresenter`、`DRT_ActivityInfoPageViewPresenter`、`DRT_ToastPopupViewPresenter`，詳見架構文件）

**2026-07-25 完成 `DRT_MatchLeaderboardDropdownParser`**（commit `077398f3df8`）：6 個換手案例維持獨立 `[Test]`（使用者這次明確指示不合併成 `[TestCase]`），全過。
- 新增 `DRT_IMatchRankingManager` 介面（只含 `GetHistoryMatchInfos()`/`GetCurrentLeaderboard()` 這兩個消費端實際會用到的方法，不是鏡射該類別全部 public 方法——這個「介面只放消費端需要的子集」原則跟 `DRT_IMatchStateManager` 只有 2 個方法（該類別實際有 4 個 public 方法）的先例一致，之後新建介面都應該先看消費端實際用到哪些方法，不要自動全部搬進去）
- **關鍵設計**：「進行中」選項的 matchId/level/date 不是另外開一條資料來源，而是從 `DRT_MatchRankingManager.GetHistoryMatchInfos()` 回傳的 `matchInfos` 裡挑 `DRT_MatchInfo.ongoing==true` 那筆；`GetCurrentLeaderboard()` 非空只當作「今天有無進行中賽事」的判斷旗標，不從裡面取值（`DRT_Leaderboard`/`DRT_MemberMatchResponse` 都沒有 matchId 欄位，天生就無法承擔這個用途）。`matchInfos` 裡 `ongoing==false` 的才是歷史選項來源
- **DTO 型別調整**：原本要求「賽事日期」用 `DateTime`，實際開發時使用者確認改用 `string`（因為 `DRT_MatchInfo.matchDate` 本身就已經是 Server 回傳的 `"MM/DD"` 格式字串，不需要额外轉型），`DRT_MatchLeaderboardDropdownOption` 最終欄位：`optionName`(string)、`matchId`(string)、`level`(int)、`matchDate`(string, "MM/DD")
- 階級中文名稱透過既有的 `DRT_IQualificationTierSettings.GetLevelName(int level)` 取得（新增這個依賴），格式化規則：「進行中」選項是 `{matchDate}{本地化文字}` 無空格（例如"01/05(進行中)"）；歷史選項是 `{matchDate} {階級中文名稱}` 有空格（例如"01/03 新銳級"）
- 新增 2 個本地化 Key（`daily_rank_tournament_ui_localize.xlsx`，用 `add_localization_key.py` 加入並執行 Unity Editor 選單 `LobbyTools/Localization/Convert All xlsx to Localization (多語系)` 轉換）：`DAILY_RANK_TOURNAMENT_MATCH_DROPDOWN_TODAY_NO_MATCH`="今日無賽事"、`DAILY_RANK_TOURNAMENT_MATCH_DROPDOWN_ONGOING`="(進行中)"，對應 `DRT_Const.LOCALIZE_KEY_MATCH_DROPDOWN_TODAY_NO_MATCH`/`LOCALIZE_KEY_MATCH_DROPDOWN_ONGOING`
- **⚠️ 這個 Convert 選單指令實測非常不可靠**：`execute_menu_item` 呼叫這個選單（一次轉換全部 64 個字表檔案）連續 3 次都回傳逾時/連線中斷錯誤（`TimeoutError`/`Unity plugin session disconnected while awaiting command_result`），但實際上 Unity 背景仍持續在跑，且**最終有成功套用**（要靠直接讀取產生的 String Table Collection asset 檔案內容才能確認，`read_console`/`editor_state` 在它执行期間會長時間 ping 不通，不能只看 RPC 回傳結果判斷成功與否）。之後遇到這個選單卡住，做法是：不要一直重複觸發（避免疊加多次重複執行拉長總時間），改成等待後直接檢查 `Assets/SourceFiles/Localization/{檔名}_zh-TW.asset` 內容（用 entry id 對照 `{檔名} Shared Data.asset` 裡的 `m_Key` 找到對應 id，再看 `_zh-TW.asset` 裡同 id 的 `m_Localized` 是否為預期文字）來確認轉換結果，比等待 RPC 回應本身可靠
- 架構文件 Public Function/依賴清單已依實作內容補齊
- 目前 25 個模塊中 **16 個已完成**，下一個依架構文件順序是 **`DRT_SeasonSettings`**（賽季設定），目前仍是純空殼，尚未換手
- **⚠️ 恢復工作時發現的未commit異動**（跟這次模塊無關）：`DRT_ActivityPeriodManager.cs` 有一份除錯用的異動，把 `CurrentActivityId`/`GetCurrentPeriodState`/`GetActivityEndTime`/`GetActivityStartTime` 都改成寫死的測試假值，導致 `DRT_ActivityPeriodManagerTest` 12 個測試失敗——**使用者已於2026-07-25確認這是他自己手動改的測試用假資料，故意保留、不用還原，測試不過沒關係**，之後遇到這 12 個失敗不用再提出詢問。`DRT_MatchPageViewPresenterTest.cs` 也有一份使用者提前寫好、尚未commit的未來模塊(`DRT_MatchPageViewPresenter`)換手案例草稿，維持不動，等真正輪到那個模塊再處理

**2026-07-24 完成 `DRT_IconViewPresenter`**（非原25模塊清單，使用者中途追加的 Icon 呈現器）：15 個測試全過。
- **⚠️ 最終定案模式（commit `9277e495ede`，取代了中途 commit `c9483b7d70c` 那版 `DRT_IconViewOpener` 包裝方案，已刪除該包裝檔案）**：`IViewsManager.Open<T>()`/`OpenAsync<T>()` 回傳的 `ViewInfo.behavior` 型別是具體 `ViewBehaviour`（MonoBehaviour 衍生類別），NSubstitute 無法 mock，這是這個專案所有既有 IconViewPresenter（BattlePass/NewbieMission/DepositActivityIcon）都沒有單元測試的原因。**正確解法不是額外包一層 Opener 介面，而是沿用系統既有的 BindView/UnbindView 回綁模式**（跟 `DRT_QualificationPageViewPresenter`/`DRT_DailyRankTournamentViewPresenter` 等頁面型 Presenter 一致）：Presenter 呼叫 `viewsManager.OpenAsync<DailyRankTournamentIconView>(this).Forget()` 觸發開啟（不需要、也不使用它的回傳值），真正建立完成後由 View 自己在 `OnOpened` 呼叫 `iconViewPresenter.BindView(this)` 回綁（`this` 是介面型別 `IDailyRankTournamentIconView`），`OnClosed` 呼叫 `UnbindView()`。測試時完全不用碰 `ViewsManager`/mock 具體 View，直接 `presenter.BindView(mockView)` 模擬「View 已綁定」即可——**之後任何 IconViewPresenter 或動態建立/銷毀型 View 的 Presenter 如果要單元測試，都用這個模式，不要用額外包裝介面**
- 這也是為什麼 `DRT_DailyRankTournamentViewPresenter` 等既有頁面 Presenter 從一開始就沒有這個 mock 問題——它們也是 BindView 模式，只是那些 View 常駐在場景裡（不像 Icon 會被動態建立/銷毀），一開始就沒人踩到這個坑
- `DailyRankTournamentIconView.cs` 原本 `OnOpened`/`OpenActivityPage` 是 TODO 佔位，這次一併補上（點擊呼叫 `iconViewPresenter.OnIconClicked()`）
- 保留中提示（`ShowKeepingHint`/`HideKeepingHint`）依使用者指示做最簡實作，就是包一個 `[SerializeField] GameObject` 的 `SetActive(true/false)`

**⚠️ 2026-07-24 修復：`DRT_MatchStateManager` 賽事ID時序漏洞**（commit `e94a4e172da`）：`OnUpdateMatchState`（「發起賽事狀態更新」事件）原本把 `MemberStatus`+`MemberMatch` 包在同一個 `DRT_ApiRequestParams` 裡送出，`matchId` 卻是送出當下（`MemberStatus` 回應之前）算好的舊值——因為 `DRT_ApiRequestHandler` 循序執行, `MemberMatch` 那筆會用到還沒更新的舊 `matchId`。修正為兩段式：先單獨發 `MemberStatus` 請求，其 callback 觸發後才用**當下最新** `GetCurrentMatchId()` 決定要不要發 `MemberMatch`（新方法 `RequestMemberMatchIfNeeded()`，`matchId` 為空/無進行中賽事則直接發 `MatchStateUpdateComplete` 跳過該請求，`OnActivityStateInitComplete` 也共用同一個守衛邏輯）。**這類「同一批 API 請求裡後面的請求需要用到前面請求回應後才會更新的資料」的情境，都不能塞進同一個 `DRT_ApiRequestParams` 批次（因為批次內欄位在送出當下就固定了，不會等前面的回應更新），要拆成前後兩段、後段的 callback 裡才組下一個請求的參數**

**⚠️ 2026-07-24 修復：`ActivityStateInitComplete`/`ModelFeatureComplete` 循環依賴死結**（commit `c1bdd69e1d4`，submodule `6cf82030a`）：
- **問題**：`DRT_ActivityPeriodManager` 原本只在收到 `ModelFeatureComplete` 時才會第一次呼叫 `NotifyActivityState()`（進而發送 `ActivityStateInitComplete`）；但 `DRT_ActivityInitializer` 要等 `ActivityStateInitComplete`（由 `DRT_ActivityPeriodManager` 發）+ `MatchStateUpdateComplete`（由 `DRT_MatchStateManager` 收到前者才會發）都到齊才會發 `ModelFeatureComplete`——兩邊互相等對方，首次啟動時永遠死結，`ModelFeatureComplete` 實際上發不出去
- **修復**：新增 `DailyRankTournamentSubEventId.InitializationComplete`（`Assets/ThirdParty/Libs/Event/YileSubEventId.cs`，`ModelExecuteFeature`跟`ModelFeatureComplete`中間），語意是「首輪初始資料已就緒」，只在**第一次**收到 `ModelExecuteFeature` 時發送一次：Close 狀態直接依序發 `InitializationComplete`→`ModelFeatureComplete`；Active/Keep 狀態則等首輪 `RequestApi`(TournamentInfo(+MemberStatus)) 的 `callback` 觸發後，**直接**依序發送 `InitializationComplete`→`ModelFeatureComplete`（`DRT_ActivityInitializer.cs` 原本這個 RequestApi 沒有帶 `callback`，現在補上）；第二次以上收到 `ModelExecuteFeature` 則不重發 `InitializationComplete`
- **⚠️ 2026-07-24 進一步簡化（commit `798d1a35df2`）**：原本設計還保留了「等 `ActivityStateInitComplete` + `MatchStateUpdateComplete` 都到齊才發 `ModelFeatureComplete`」這層 cascade 判斷（`hasReceivedActivityStateInitComplete`/`hasReceivedMatchStateUpdateComplete`/`isWaitingForFeatureComplete`/`CheckFeatureCompleteReady()`），但使用者確認**不需要這層等待**——`ModelFeatureComplete` 現在跟 `InitializationComplete` 在同一個 callback 裡直接連續發送，`DRT_ActivityInitializer` 已完全不監聽 `ActivityStateInitComplete`/`MatchStateUpdateComplete` 這兩個事件（相關欄位、方法、event case 全部移除）。`DRT_MatchStateManager` 仍然會監聽 `ActivityStateInitComplete` 做自己的事（跟 `ModelFeatureComplete` 的時序脫鉤，純粹各自獨立運作）
- `DRT_ActivityPeriodManager` 的 `OnReceiveEvent` 改成監聽 `InitializationComplete`（方法改名 `OnInitializationComplete`），不再監聽 `ModelFeatureComplete`；`DailyRankTournamentModel.cs` 本身仍監聽 `ModelFeatureComplete` 觸發 `OnFeatureComplete()`，不受影響
- **這是通用的架構風險，日後其他模塊如果也設計成「等待某個 xxxComplete 事件才做初始化」，要注意這個 xxxComplete 事件本身的其中一個前置條件是否恰好由「這個正在等待的模塊自己」發送，若是就會死結，需要拆出一個更早、不依賴下游事件的獨立信號**

**⚠️ 2026-07-23 起：新規範——const 一律集中放 `DRT_Const`**（commit `afd8353c64d`，已把 `DRT_MatchTimeManager`/`DRT_MatchStateManager`/`DRT_ActivityPeriodManager`/`DRT_QualificationTimeManager` 原本各自宣告的 const 全部搬過去）：之後新模塊若有倒數計時器key、狀態字串（如"YES"/"RETENTION"）、delay秒數等 const，直接寫進 `DRT_Const.cs`（`Assets/Scripts/Lobby/Activity/DailyRankTournament/DRT_Const.cs`），不要在模塊自己的 class 裡宣告 `private/public const`；測試檔也直接引用 `DRT_Const.XXX`，不要在測試類別裡另外重複宣告一份同名 const。命名要帶模塊語境前綴避免集中後撞名（例如 `COUNT_DOWN_KEY_ACTIVITY_PERIOD_TO_CLOSE` 而非泛用的 `COUNT_DOWN_KEY_TO_CLOSE`）。

**2026-07-25 迭代 `DRT_QualificationPageViewPresenter`**（commit `1a4c4772275`，透過「迭代模塊」獨立觸發，分支B修改現有class）：改變事件註冊/取消註冊的時機。
- **⚠️ 通用架構模式（其餘 Page ViewPresenter 如 `DRT_MatchPageViewPresenter`/`DRT_SeasonPageViewPresenter` 之後實作時應沿用）**：不能真的讓 `yileEventAdapter` 的 register/unregister 跟著 `SwitchTab`（切換頁籤）事件動態開關——若真的在收到「切走」時呼叫 `UnregisterEvent()`，這個 class 會從此再也收不到任何 `DailyRankTournament` 事件（包括下一次切回來的 `SwitchTab` 事件本身），永久卡死收不到通知。正確作法（比照既有 `DRT_MatchLeaderboardAutoRefresher` 的 `isMatchTabOpen` 寫法）：`yileEventAdapter.RegisterEvent(...)` 改成在 `Initialize()` 就永久訂閱一次，`ShowPage()`/`ClosePage()` 不再各自呼叫 register/unregister；「監聽開關」改用既有的 `isPageOpen` 欄位本身當 flag（不需要另外新增欄位），新增 `SwitchTab` case：`targetTab==Qualification` 時設 `isPageOpen=true`，否則設 `false`；`CloseActivityPage` case 也順便設 `isPageOpen=false`。原本會觸發刷新的 `MemberQualificationResponse`/`UpdateActivityState` case 不用額外加判斷，因為 `RefreshPage()` 內部本來就有 `if (!isPageOpen) return;` 守衛，天然就會被這個共用 flag 擋下來
- 舊有「顯示頁面後註冊事件」/「關閉頁面後取消註冊事件」這兩個直接呼叫 `presenter.ShowPage()/ClosePage()` 斷言 adapter register/unregister 次數的測試已被使用者刪除，改成 3 個新案例驗證「透過 SwitchTab/CloseActivityPage 事件間接影響刷新行為」（用 `view.Received/DidNotReceive` 驗證有無刷新，不是直接斷言 adapter 呼叫次數）
- **⚠️ 2026-07-25 使用者發現的真實bug（commit `5b25715abe6`）**：改成「Initialize永久註冊一次」之後，原本 `UnbindView()` 裡呼叫的 `UnregisterEvent()`（真的呼叫 `yileEventAdapter.UnregisterEvent(this)`）變成必須移除——`UnbindView()` 是玩家**每次**關閉整個活動頁面（`DailyRankTournamentView.OnClosed` → `qualificationPageViewPresenter.UnbindView()`）都會執行的，但 `Initialize()` 只在 Model 生命週期裡執行一次（不會因為玩家重新開頁面而再跑一次），兩者搭配會導致玩家只要關過一次頁面，訂閱就被永久取消、之後永遠收不到事件。**這類「永久註冊」模式的通用規則：只要某個 view-bound presenter 改成 Initialize 永久註冊，就要順便檢查它的 `UnbindView()`／`ClosePage()` 等生命週期方法裡是否還殘留真的呼叫 adapter `UnregisterEvent()` 的地方，若該方法會被重複呼叫（多次開關頁面）但沒有對應的重複 Initialize，就必須移除，只留 flag 開關，不要真的解除訂閱**
- **⚠️ 這類「永久註冊+內部flag開關」案例的換手敘述命名慣例（commit `480784975f5`）**：不要寫「註冊事件」/「取消註冊事件」（會讓人誤以為在斷言 adapter 的 RegisterEvent/UnregisterEvent 呼叫），改用「之後收到事件時會正常執行動作」/「之後收到任何事件皆不會執行任何動作」這種泛稱寫法——好處是之後這個模塊若擴充監聽更多種事件，敘述不用跟著改（不用寫死是哪個特定事件），之後其他 ViewPresenter 遇到同樣的 SwitchTab-driven 開關案例，換手案例命名都比照這個措辭

**2026-07-25 迭代 `DRT_MatchLeaderboardAutoRefresher`**（commit `0f0a2b7736b`，submodule ThirdParty `4e247add3`，**payload後續又改了兩次，詳見下方兩則**）：倒數計時到、API callback success時，除了開下一輪倒數，還新增發送 `DailyRankTournamentSubEventId.RefreshLeaderboardPeriodically`(帶當前賽事ID字串) 事件；新SubEventId加在submodule `Assets/ThirdParty/Libs/Event/YileSubEventId.cs`，照慣例submodule先commit拿SHA、主專案commit message記錄該SHA。18個測試全過。（⚠️ SubEventId本身`RefreshLeaderboardPeriodically`維持不變，只有事件的payload型別後續改了兩次，最終版見下方 `DRT_ApiRequestResult` 那則）

**2026-07-25 重大共用基礎設施異動：`DRT_ApiRequestParams.callback` 改帶 `DRT_ApiRequestResult`**（commit `55e19762ce3`）——這是使用者跟我討論後決定的架構調整，**影響全部之後新模塊**，務必記住新慣例：
- 舊：`Action<bool> callback`（只有整體成功與否）。新：`Action<DRT_ApiRequestResult> callback`，`DRT_ApiRequestResult` 有 `success`(bool，語意不變) + `T GetResponse<T>() where T : ServerCommonData`(依型別從內部 `Dictionary<Type, ServerCommonData>` 撈，撈不到回 null)
- 動機：原本很多模塊（如 AutoRefresher）想要 API 回應後的最新資料時，只能額外依賴另一個「狀態管理模塊」的 getter（例如 `DRT_MatchRankingManager`），即使該模塊自己就發了這個請求也一樣；改成 `GetResponse<T>()` 後可以直接在 callback 拿到自己剛請求那筆的 response，不用為此多繞一層依賴
- `DRT_ApiRequestHandler.ExecuteSequential`/`FinishAll` 內部改成建立並累積一個 `DRT_ApiRequestResult`（而非單純 bool），`HandleResponse<T>` 成功時額外把 `response` 存進去；即使一次請求帶多個 `apiTypes`，各自的 response 都會分別存好，之後用對應型別各自取
- 一次性更新了 5 個既有 consumer：`DRT_ActivityInitializer`(callback用`_`丟棄，完全不用改)、`DRT_MatchLeaderboardAutoRefresher`(現在直接用`result.GetResponse<DRT_MemberMatchResponse>()`組"週期刷新排行榜"事件參數，不再需要注入`DRT_IMatchRankingManager`)、`DRT_MatchRankingManager`(callback內部邏輯不變，只是簽名從`success`改`result.success`)、`DRT_MatchStateManager`(同樣只改簽名)、`DRT_QualificationPageViewPresenter`(`RequestOpenPage(Action<bool> callback)`這個對外公開的介面完全不變，只在內部包一層`result => callback?.Invoke(result.success)`)
- **⚠️ 測試檔案地毯式踩雷點**：所有測試檔案原本的 `CallRequestApiCallback(bool success) { capturedRequestParams?.callback?.Invoke(success); }` 這個共用helper樣式，全部要改成建立 `DRT_ApiRequestResult`才能呼叫；已更新 `DRT_ActivityInitializerTest`/`DRT_ApiRequestHandlerTest`/`DRT_MatchLeaderboardAutoRefresherTest`/`DRT_MatchRankingManagerTest`/`DRT_MatchStateManagerTest`/`DRT_QualificationPageViewPresenterTest` 六份，helper統一改成 `CallRequestApiCallback(bool success, params ServerCommonData[] responses)`；`DRT_QualificationPageViewPresenterTest` 沒有共用helper、是直接inline呼叫 `capturedRequestParams.callback.Invoke(...)`，也要記得改成 `new DRT_ApiRequestResult { success = true }`
- 之後任何新模塊要用 `DRT_ApiRequestParams.callback` 都要遵循這個新簽名；新增測試也要用上述新版helper樣式，不要再寫舊的 `Action<bool>` 假設
- `DRT_ApiRequestHandlerTest` 新增一個測試直接驗證 `GetResponse<T>()` 在核心 handler 真實流程下的正確性(成功回對應response、失敗回null)

**2026-07-25 再次迭代 `DRT_MatchLeaderboardAutoRefresher`**（commit `1b992df3363`，**⚠️此版本的 `DRT_IMatchRankingManager` 依賴後續已被移除、下一則記錄取代**）：`RefreshLeaderboardPeriodically` 事件參數再改一次，不帶賽事ID字串，改帶新DTO `DRT_LeaderboardRefreshEventParams`(`memberSelf`:自己榜單`DRT_Leaderboard`、`leaderboard`:排行榜列表`List<DRT_Leaderboard>`)——新增依賴 `DRT_IMatchRankingManager`(介面補上 `GetCurrentMemberLeaderboard()`)，直接呼叫 `GetCurrentMemberLeaderboard()`/`GetCurrentLeaderboard()` 取值。18個測試全過。使用者這次也提醒了「測試案例method name沒跟著comment一起改」——修改測試案例時要記得連同方法命名一起檢查是否還符合新的敘述，不要只改註解內容。（**後續動態**：使用者接著要求改成直接從 API callback 的 response 拿資料，不透過 `matchRankingManager`，促成了下方 `DRT_ApiRequestResult` 這個更大範圍的基礎設施重構；`DRT_IMatchRankingManager` 的 `GetCurrentMemberLeaderboard()` 介面方法與 AutoRefresher 對它的依賴都已經還原移除，`DRT_LeaderboardRefreshEventParams` 這個DTO本身則保留沿用）

**2026-07-25 再次迭代 `DRT_MatchStateManager`**（commit `e8254191d01`）：把 `UpdateMatch` 進一步拆成 `UpdateSameDayMatch`/`UpdateNewDayMatch`——`OnUpdateMatchState()` 在發送`MemberStatus`請求**之前**先用 `GetCurrentMatchId()` 存一份 `previousMatchId`，等 `MemberStatusResponse` 更新完（會員狀態刷新後 `GetCurrentMatchId()` 已是新值）在 callback 裡比較新舊 matchId 是否相同決定 successType，再傳給既有的 `RequestMemberMatchIfNeeded(successType)`（沒改這個方法簽名）。27個測試全過。連帶把既有那個測試(`...with_fresh_match_id...`)的 setup 補上一筆換手前 `CallMemberStatusResponseEvent` 建立「舊」matchId，原本它沒有任何前置資料，同一個matchId反而測不出「未改變」的情境。

**2026-07-25 迭代 `DRT_MatchStateManager`**（commit `ca8b82fd5f3`，透過「迭代模塊」獨立觸發，分支B修改現有class）："賽事狀態更新完成"(`MatchStateUpdateComplete`)事件新增`DRT_MatchStateUpdateType`enum參數(`NoUpdate`/`InitializeNewMatch`/`UpdateMatch`)：`ActivityStateInitComplete`流程成功時為`InitializeNewMatch`，`UpdateMatchState`流程成功時為`UpdateMatch`，其餘情境(非Active狀態/無進行中賽事/matchId為空/API callback失敗)一律`NoUpdate`。26個測試全過(24舊+2新)。
- **⚠️ 幫既有「無參數事件」加data參數時的通用陷阱**：`SendCommonEvent(eventId, subEventId, data=null, action=null)`是帶預設值的單一方法，舊測試寫 `.Received(1).SendCommonEvent(eventId, subEventId)`(省略後面參數)在C#編譯期就等同於顯式傳 `data:null`，一旦production code開始帶真正的data(enum非null)，這些舊斷言會全部悄悄失敗匹配。**這類異動要地毯式檢查該事件在測試檔裡所有 `.Received()`/`.DidNotReceive()`/`.When()` 用法**，`.Received()`/`.DidNotReceive()` 要嘛補上正確的期望值，要嘛用 `Arg.Any<object>()`；特別是 `.When(x => x.SendCommonEvent(...)).Do(...)` 這種只是用來截取callback時機的寫法最容易漏改——沒補 `Arg.Any<object>()` 的話該 `.Do()` 永遠不會被觸發，測試會安靜地變成「沒有真的斷言到任何東西」的假陽性，而不是編譯錯誤或執行失敗，非常隱蔽

**2026-07-25 迭代 `DRT_MatchRankingManager.RequestMatchLeaderboard`**（commit `a18fb4e0fe4`）：callback 新增自己榜單資料，選方案A（雙參數 `Action<List<DRT_Leaderboard>, DRT_Leaderboard>`，排行榜列表在前、自己榜單在後），沒有選用另一個備案（沿用 `DRT_LeaderboardRefreshEventParams` 包裝類別）。`historyLeaderboardCache` 的value型別從 `List<DRT_Leaderboard>` 改成 tuple `(List<DRT_Leaderboard> leaderboard, DRT_Leaderboard memberSelf)`。順便用 `result.GetResponse<DRT_MemberMatchResponse>()`（`DRT_ApiRequestResult`）取代了原本靠監聽 `MemberMatchResponse` domain event 才更新的 `latestMemberMatchResponse` 欄位（該欄位因此整個刪除）——這類「callback內就能直接拿到這次請求的response」的場景，之後都優先用 `GetResponse<T>()`，不要再另外監聽 domain event 存欄位。**改動前有先查證這個方法目前沒有任何生產程式碼呼叫**（只有它自己的測試在用），所以這次異動範圍完全侷限在這一個檔案+測試，沒有波及其他模塊；之後遇到類似「evaluate 改動範圍」的請求，記得先 grep 確認實際 consumer 數量再回答。17個測試全過。

**2026-07-25 新增模塊空殼 `DRT_MatchLeaderboardViewPresenter`**（commit `b3672717e48`，非原25模塊清單，使用者中途追加、透過「迭代模塊」分支A新增）：「賽事排行榜呈現器（ViewPresenter）」，用途概述「賽事頁面排行榜顯示流程，以及歷史榜單下拉式選單顯示和切換流程」，架構文件已登記（是否為待處理：是），class本體與測試檔皆仍是純空殼，**尚未換手**。同時把排行榜相關職責從 `DRT_MatchPageViewPresenter` 的用途概述移除，該模塊新描述改為「開啟頁面前請求賽事資料，開啟後更新除排行榜以外的賽事頁畫面」（`DRT_MatchPageViewPresenter` 本身也還是空殼，待處理）。下次恢復工作若使用者要繼續處理這個新模塊，需要先請使用者在 `DRT_MatchLeaderboardViewPresenterTest.cs` 用註解列出測試案例（換手），再走「實作前檢查」三項。

**2026-08-03 現況核實（讀架構文件原文，非記憶推測）**：架構文件實際登記 29 個模塊（原25個 + 中途追加的 `DRT_ApiRequestHandler`/`DRT_RedPointHandler`/`DRT_IconViewPresenter`/`DRT_MainViewPresenter`/`DRT_RewardInfoParser`，扣掉已移除的 `DRT_ActivityTimeUpdater`），不是「25個」。「是否為待處理：是」目前剩 8 個：`DRT_SeasonTimeManager`（下一個，已有預寫換手草稿）、`DRT_SeasonStateManager`、`DRT_SeasonRankingManager`、`DRT_FirstOpenTrigger`、`DRT_ToastPopupManager`、`DRT_ToastPopupQueueHandler`、`DRT_SeasonPageViewPresenter`、`DRT_ToastPopupViewPresenter`。

**2026-08-03 完成 `DRT_SeasonSettings`**（commit `98b885d783d`）：11 個測試全過（8個換手案例中「取得指定名次的獎勵資料...正確獎勵資料」改成 `[TestCase]` 參數化，涵蓋名次1/3/4/99，故實際11條）。
- `IsSeasonRankingVisible()` 改讀 `DRT_TournamentInfoResponse.seasonEnabled`（監聽 `TournamentInfoResponse`，即「活動基本設定API回應」），不再寫死 `false`
- `GetSeasonRewards()`/新增的 `GetSeasonRankReward(int rank)` 都讀 `DRT_RewardInfoResponse.seasonRewards`（監聽 `RewardInfoResponse`），名次區間比對邏輯照抄 `DRT_MatchSettings.IsRankInRange`
- **排行榜最大顯示數量不是Server資料**，新建 `DRT_ISeasonLeaderboardSetting`（只有 `GetDisplayRankCount()` 一個方法，跟賽事榜的 `DRT_IMatchLeaderboardSetting` 不同，賽季榜不需要刷新頻率）+ `DRT_SeasonLeaderboardSO`，資產 `Assets/SourceFiles/DailyRankTournament/ScriptableObject/SeasonLeaderboardSetting.asset`（`UIPrefabsDefine.DRT_SeasonLeaderboardSetting`）
- **⚠️ 換手案例明確指定「若無資料則回傳0」**，跟 `DRT_MatchSettings.GetDisplayRankCount()` 的預設值 `-1` 不同——之後遇到類似「無資料時回傳什麼」的模塊，不要預設沿用其他模塊的慣例，以換手案例文字為準
- **⚠️ 這次 Unity MCP 連線一開始處於「還在連線中」狀態**，工具要等重連後（`/mcp` 指令）才出現；`refresh_unity(wait_for_ready=true)` 第一次呼叫時 Unity 正好在 compiling，等了60秒才逾時，但實際上背景真的有在跑，重新呼叫一次（`compile:none`）就正常回報`ready`——這跟先前記錄過的 Convert Localization 選單不可靠情況是同一類「RPC逾時不代表沒在跑」模式，之後遇到類似情況先重試一次再判斷是否真的卡住
- **⚠️ 這次資產是純手動建立**：因為 Unity MCP 一開始連不上，`DRT_SeasonLeaderboardSO.cs`/`DRT_ISeasonLeaderboardSetting.cs` 的 `.meta` 檔（含 GUID）跟 `SeasonLeaderboardSetting.asset` 本體都是我手動生成 GUID 寫入的（用 PowerShell `[guid]::NewGuid()`），不是透過 Unity Editor 建立——事後 Unity 重新連線 compile 通過、測試全過，證實這個手動建立 SO 資產的作法在這個專案是可行的，之後如果又遇到 Unity MCP 斷線但急需建立新 SO 資產，可以比照這個做法（手動編兩個GUID分別給script跟asset的.meta，asset本體YAML照抄既有同類SO的格式）
- commit 只包含這次模塊相關的12個檔案，特別排除了同一時間 Unity 背景刷新順便touch到的多個不相關檔案（`AddressableAssetsData/AssetGroups/DailyRankTournament.asset`、`PreLogin.unity`、`ViewsManagerSetting.asset`、字型SDF asset等）——這些應該是Unity Editor重新整理時的正常序列化格式變動，不是這次改動造成的，commit時要留意排除避免混進不相關異動

**2026-08-03 完成 `DRT_SeasonTimeManager`**（commit `b15084c15a5`，submodule ThirdParty `94e3c2f93`）：12 個測試全過。
- 換手案例明確指出「賽季起訖時間」＝「第一場賽事開始時間~最後一場賽事結束時間」，來自「活動基本設定API回應」（`TournamentInfoResponse`），但**目前資料結構還沒有這些欄位**——已在 `DRT_TournamentInfoResponse` 先聲明4個占位long欄位（`firstMatchStartTime`/`firstMatchEndTime`/`lastMatchStartTime`/`lastMatchEndTime`，皆標TODO註解待Server正式欄位名稱確認），`GetSeasonPeriodTimeInfo()` 只實際用到 `firstMatchStartTime`（起）與 `lastMatchEndTime`（迄）
- **⚠️ 接口異動**：`DRT_ISeasonTimeManager.GetSeasonPeriodText()` 已移除，改成 `ActivityTimeInfo GetSeasonPeriodTimeInfo()`，字串格式化流程移到 `DRT_ActivityInfoPageViewPresenter.FormatSeasonPeriod()`（`"yyyy/MM/dd HH:mm:ss ~ yyyy/MM/dd HH:mm:ss"`，用 `long.ToDateTime().ToUTC8DateTime().ToString(...)`，這對 extension methods 定義在 `Assets/ThirdParty/SCG_Script/CustomTimeExtension.cs`，全域無namespace）——之後其他模塊如果也要把「時間長整數轉顯示字串」的職責放到哪一層，這是一個先例：底層 Manager 只回傳 `ActivityTimeInfo`（原始資料），格式化留給 Presenter 層
- **新建 `DRT_ISeasonStateManager`**（只有 `bool IsSeasonOngoing()` 一個方法）+ `DRT_SeasonStateManager` 最小 stub（永遠 `return false`，無事件監聽）——這是「賽事狀態從尚未實作的模塊拿，請抽interface並補上最小實作」這類需求的處理模式先例：只建最小介面滿足依賴注入，不因此把該模塊標記為已完成（`DRT_SeasonStateManager` 架構文件仍是「是否為待處理：是」）
- **新增2個 SubEventId**（submodule `ThirdParty`）：`UpdateSeasonState`（發起賽季狀態更新）、`SeasonStateUpdateComplete`（賽季狀態更新完成）——設計上跟 `DRT_MatchTimeManager` 的對應機制不同：Match版本無論賽事狀態為何都經過`RequestUpdateMatchStateAsync`统一判斷頁面是否開啟；但這次換手案例明確要求「若當前賽事狀態為進行中」時**無視頁面開啟與否，一律立即發送**，只有「尚未開始」狀態才會走「頁面未開啟→隨機延遲1~10秒／頁面開啟中→立即發送」的邏輯，兩個模塊的行為不能照抄
- **⚠️ 換手案例第3條疑似筆誤**：「取得賽季起訖時間(ActivityTimeInfo), 若無資料, 則返回正確值」跟第2條「若無資料, 則返回null」重複，已跟使用者確認過（隱含在後續對話中未被糾正）並判斷應為「若**有**資料」，照這個理解實作與寫測試
- **⚠️ 2026-08-03 事後簡化（commit `9bc64d476fd`）**：使用者評估後認為「倒數計時器時間到」的4個分支案例（進行中/尚未開始+頁面開關/取消延遲）可以合併，改成「不論賽季狀態或活動頁面開啟與否，一律立即發送」，拿掉了 `IRandomDelayProvider`/`DRT_IMainViewPresenter` 依賴、`PrepareOpenActivityPage` 監聽、`REQUEST_UPDATE_SEASON_STATE_MIN/MAX_DELAY_SECONDS` 這兩個const都跟著刪除（確認沒有其他地方引用才刪）。`DRT_SeasonTimeManager.Initialize` 簽名也跟著簡化（少了兩個參數）。**這是跟 `DRT_MatchTimeManager` 行為分道揚鑣的關鍵案例**——之後如果需要參考「倒數計時到點後的後續動作」該抄哪個模塊，要先確認清楚是哪一種模式，不要預設兩者一致
- **⚠️ 2026-08-03 再次調整（commit `e803bd99c70`，隨後又被下一則取代）**：上面「立即發送」先改成「固定延遲1秒後才發送」，第一版實作是複用 `ITimeManager.SetCountDownListener` 再包一層（倒數計時時間到→再排一個1秒的 `SetCountDownListener`），新增過 `DRT_Const.COUNT_DOWN_KEY_SEASON_STATE_UPDATE_DELAY`（**已在下一版移除，不要再用這個名稱**）
- **⚠️ 2026-08-03 最終版（commit `2a4d8a36b46`）**：使用者明確要求「延遲1秒請用UniTask實作」，改成重新引入 `IRandomDelayProvider`（`Initialize` 簽名補回這個參數，在 `min=max=1` 的方式呼叫 `DelayRandomSeconds(1, 1)`，讓「固定延遲」直接沿用專案既有的 `IRandomDelayProvider`（本質是UniTask包裝）而不是發明新機制。**這是本次「延遲1秒」需求的最終定案版本**：`SendUpdateSeasonStateAfterDelayAsync()`（`async UniTaskVoid`）await `randomDelayProvider.DelayRandomSeconds(...)` 後才送事件；測試沿用專案既有的 `randomDelayProvider.DelayRandomSeconds(...).Returns(UniTask.CompletedTask)` 讓await同步完成的標準mock手法，不需要真的等待，也不需要「兩段式倒數」的驗證寫法（上一版那套已作廢）
- **⚠️ 「固定延遲」該用什麼機制的決策脈絡**：這次繞了兩圈（倒數計時器包一層→改用IRandomDelayProvider），之後若再遇到「固定N秒後做某事」的需求，**直接問清楚或優先選 `IRandomDelayProvider`（min=max=N）**，因為這是專案唯一已建立、可mock、測試不用真等待的UniTask延遲管道；不要預設用倒數計時器機制去模擬，那是給「有實際到期時間點」的倒數計時場景用的（如賽季起訖時間），語意不同
- **2026-08-03 已處理**：`DRT_MatchTimeManager` 的「活動頁面已開啟→立即發送」改成「延遲1秒後發送」（commit `08a72fc0768`），跟 `DRT_SeasonTimeManager` 一樣用 `randomDelayProvider.DelayRandomSeconds(1, 1)`（新增 `DRT_Const.REQUEST_UPDATE_MATCH_STATE_ACTIVITY_PAGE_OPEN_DELAY_SECONDS`），「頁面未開啟」那支既有的隨機1~10秒延遲+`PrepareOpenActivityPage`取消邏輯完全不動。兩個測試方法名稱也一併從 `..._then_send_update_match_state_event_immediately` 改成 `..._then_delay_and_send_update_match_state_event`（呼應之前使用者提醒過的「test function name和測試內容也要一起改」規則）。467個測試全過

**2026-08-04 完成 `DRT_SeasonStateManager`**（commit `807e2cbcc5f`）：7個測試全過，474個DailyRankTournament測試全過。
- `IsSeasonOngoing()`：`DRT_ISeasonTimeManager.GetSeasonPeriodTimeInfo()` 拿賽季起訖時間，配合 `ITimeManager.GetCurrentTime` 判斷當前時間是否落在區間內（無資料則false）——這是本模塊唯一的查詢方法
- 監聽 `ActivityStateInitComplete`（不檢查其帶的 `ActivityState` 參數值，收到就直接發 `SeasonStateUpdateComplete`）與 `UpdateSeasonState`：後者依 `DRT_IMainViewPresenter.IsViewOpened` 決定——頁面未開啟直接發 `SeasonStateUpdateComplete`；頁面開啟中則先發 `RequestApi`(`DRT_ApiType.SeasonLeaderboard`)，callback後才發 `SeasonStateUpdateComplete`（不論API success與否都送，沒有像 `DRT_MatchStateManager` 那樣依 success 分岔更新類型，因為 `SeasonStateUpdateComplete` 本身不帶參數）
- 模式跟 `DRT_MatchStateManager` 的 `RequestApi`+`callback`+`GetResponse<T>()` 是同一套（`DRT_ApiRequestResult`），寫法幾乎可以照抄，只是這次沒有"matchId 是否改變"這類額外判斷，單純得多
- `DailyRankTournamentModel.cs` 裡 `seasonStateManager` 欄位型別從 `DRT_ISeasonStateManager`（先前只是最小stub時的型別）改回具體類別 `DRT_SeasonStateManager`（因為現在要呼叫它自己的 `Initialize(...)`，`Initialize` 不算在interface裡）——**這是「先建最小stub interface型別欄位、之後真正實作時要記得把欄位型別改回具體類別」的通用提醒**，之後遇到類似先前佔位的模塊也要注意這點，不能一直留著interface型別欄位
- **⚠️ 踩雷**：`ActivityTimeInfo` 在 `SCGLobby.DataStruct` namespace，一開始漏加這個using導致編譯錯誤（CS0246），Unity MCP第一次 `run_tests` 因為編譯失敗，實際上是拿舊組件在跑（測試數量沒變化、也沒有失敗），這是一個容易誤判「測試過了」的假象——**之後執行測試前一定要先看 `read_console` 有沒有 error，數量沒變化又全過很可能代表根本沒編譯進新程式碼，不能只看 run_tests 的 summary 就判斷成功**
- 恢復工作時發現 `DRT_SeasonRankingManagerTest.cs` 又有使用者已預先寫好但尚未commit的換手草稿（下一個待處理模塊），維持不動

**2026-08-04 完成 `DRT_SeasonRankingManager`**（commit `c325d7b3d4f`）：10個測試全過，490個DailyRankTournament測試全過。
- 新建 `DRT_ISeasonRankingManager`（`GetSeasonLeaderboard()`/`GetSeasonMemberLeaderboard()`），依賴 `DRT_ISeasonStateManager`(判斷是否進行中)+`DRT_ISeasonSettings`(取得顯示上限`GetDisplayRankCount()`)，監聽 `SeasonLeaderboardResponse`（StatusCode 27301時清空兩個欄位，否則直接存 `response.leaderboard`/`response.memberSelf`，不做任何排序）
- **⚠️ 換手案例明確要求「不另外排序，完全以server列表順序顯示，超過上限從尾部截斷」**：截斷邏輯是 `leaderboard.GetRange(0, displayRankCount)`（保留前N筆、砍掉後面），不是常見誤解的「從陣列尾端往前數N筆」——之後遇到類似「從尾部截斷」的敘述，要先確認是「保留前段砍尾段」還是「保留尾段砍前段」，這次確認是前者
- **⚠️ 踩雷**：一開始 `receive_season_leaderboard_response_then_update_data` 測試忘記呼叫 `GivenDisplayRankCount(...)`，NSubstitute對未設定的int mock預設回傳0，導致 `GetRange(0,0)` 回傳空列表讓斷言「應該有1筆」失敗——**任何會呼叫 `GetDisplayRankCount()` 的測試案例，只要斷言列表筆數大於0，都要記得先設定這個mock，不能倚賴其他測試案例剛好設過**
- **2026-08-04 重大改版：`IsSeasonOngoing()`(bool) → `GetSeasonState()`(enum) `DRT_SeasonState{NotStarted,Ongoing,Ended}`**（commit `b104586034c`）：使用者發現賽季需要「已結束」第三種狀態，bool不敷使用。502個DailyRankTournament測試全過。
  - `DRT_SeasonStateManager.GetSeasonState()` 現在是**即時運算**（不再是快取欄位）：`seasonTimeManager.GetSeasonPeriodTimeInfo()`+`timeManager.GetCurrentTime` 即時比較，即使從未收到過 `SeasonLeaderboardResponse` 事件也能正確回答（尚未開始/進行中/已結束）。27301(賽季尚無任何積分) 只是一個**覆寫旗標**（`hasNoSeasonScore`），命中時強制回傳 `NotStarted`，不受時間比較影響；收到 success response 會清除這個旗標，讓時間比較恢復主導
  - **⚠️ `UpdateSeasonState` 三態分支邏輯（重要，不要憑直覺重新設計）**：
    1. `Ended`（不論頁面開關）→固定延遲1秒→直接發 `SeasonStateUpdateComplete`，**不打API**
    2. 非Ended + 頁面開啟（`NotStarted`或`Ongoing`皆同）→立即打 `RequestApi(SeasonLeaderboard)`
    3. `Ongoing` + 頁面未開啟→固定延遲1秒→直接發`SeasonStateUpdateComplete`，不打API
    4. `NotStarted` + 頁面未開啟→隨機延遲1~10秒(可被`PrepareOpenActivityPage`取消)→打`RequestApi`
    - 判斷順序必須先檢查`Ended`（因為它會覆蓋頁面開啟與否的判斷），再檢查頁面開啟，最後才分`Ongoing`/`NotStarted`
  - **⚠️ `DRT_SeasonRankingManager`/`DRT_SeasonTimeManager` 這兩個既有consumer的行為選擇是我主動推斷、非明確要求**：
    - `DRT_SeasonRankingManager`：一開始誤寫成「只有Ongoing才顯示榜單」，後來根據換手案例的更新註解（"進行中或已結束...則返回完整列表"）才確認正確語意其實是「**Ongoing跟Ended都顯示榜單**，只有NotStarted隱藏」——賽季結束後應該還能看到最終排行榜結果，這是合理的UX，之後遇到類似"是否顯示資料"的問題，記得已結束不等於要隱藏
    - `DRT_SeasonTimeManager`：`OnSeasonStateUpdateComplete()` 原本二選一分支（Ongoing→倒數到end / else→倒數到start）維持不變邏輯，`Ended`狀態目前歸類在「else」分支（等同NotStarted一樣倒數至start）——這只是為了讓舊測試維持通過的保守選擇，**沒有專門的Ended測試案例驗證這個選擇是否才是使用者真正想要的行為**，之後如果要處理`DRT_SeasonTimeManager`的下一輪迭代，記得跟使用者確認「賽季已結束後」該不該真的倒數回下一次賽季開始，還是有其他機制
  - **2026-08-04 事後修正（commit `cef2954a881`）**：`DRT_SeasonTimeManager.OnSeasonStateUpdateComplete()` 原本用 `activityPeriodManager.GetCurrentPeriodState() != ActivityState.Active` 判斷「保留中/已結束時不啟動任何倒數計時器」，使用者要求改成用 `seasonStateManager.GetSeasonState() == DRT_SeasonState.Ended` 判斷，並整個拔掉 `IActivityPeriod` 依賴（`Initialize` 少一個參數）。508個測試全過。**這是activity層級狀態(Keep/Close)跟賽季層級狀態(Ended)語意混淆的案例**——之後若其他賽季相關模塊也還殘留用 `ActivityState`/`IActivityPeriod` 判斷「是否結束」的邏輯，優先確認是否該改用 `DRT_SeasonState` 判斷才對，這兩層狀態不是同一件事
- **⚠️ 這次調整期間，多個測試檔案（`DRT_SeasonStateManagerTest`/`DRT_SeasonTimeManagerTest`/`DRT_SeasonRankingManagerTest`）被使用者在IDE端同步修改註解，跟我的Edit工具呼叫互相競爭**，導致其中一次我對`DRT_SeasonTimeManagerTest.cs`的Edit「顯示成功」但實際沒有生效（下次Read發現內容還是舊的）——**之後遇到使用者可能正在同時手動編輯測試檔案的情境，每次要修改前都先重新Read該檔案確認最新內容，不要相信Edit工具回傳成功就代表真的寫入了，尤其是連續處理多個檔案時**

- **⚠️ 2026-08-04 事後修正（commit `b2624929bf1`）**：`DRT_SeasonRankingManagerTest.cs` 的 memberId 測試資料一開始寫成 `"M1"/"M2"/"M3"/"Self"`，後來又誤改成 `"P001"/"SELF"`（照抄 `DRT_MatchRankingManagerTest.cs` 的舊寫法），**兩次都錯**——正確規則記載在 `aiToolLib/.claude/rules/14-unit-test.md` 第243-252行：memberId 測試資料一律用 `"JCG"+10位數字` 格式（例如 `"JCG0000000001"`），`DRT_MatchPageViewPresenterTest.cs` 裡的 `"JCG0000000001"` 才是遵循規則的正確範例。**之後任何新測試檔案要編 memberId 測試資料，第一步就該去查 `rules/14-unit-test.md`，不要憑感覺照抄同資料夾裡隨便一個既有測試檔案**——舊測試檔案可能寫在規則訂立之前，不能當作可靠的規則來源
- 恢復工作時發現 `DRT_SeasonPageViewPresenterTest.cs` 也已有使用者預先寫好但尚未commit的換手草稿（架構文件順序上排在 `DRT_FirstOpenTrigger`/`DRT_ToastPopupManager`/`DRT_ToastPopupQueueHandler`/`DRT_ToastPopupViewPresenter` 之後，不是下一個，維持不動）；另外 Server API 文件 `_分組日榜 DailyRankTournament.xlsx` 似乎也有異動但未commit，跟這次模塊無關沒有處理

**2026-08-04 `DRT_SeasonStateManager` 大幅改版**（commit `ff0464c4d55`）：使用者事後評估流程需要調整，`IsSeasonOngoing()` 從「每次查詢時即時用 `seasonTimeManager`+`timeManager` 計算」改成「由收到 `SeasonLeaderboardResponse` 事件時計算並儲存成欄位，查詢時單純回傳欄位」。480個DailyRankTournament測試全過。
- **關鍵新增：`DRT_ApiRequestHandler.HandleResponse<T>` 新增 `params int[] exceptionStatusCodes` 參數**——這是本次最重要的共用基礎設施異動。原本任何 `retstatus.StatusCode != successCode` 一律視為失敗（不送事件、不傳response）；現在命中例外碼時改成「跳過 `EnqueueActivityErrorData`/`ParseErrorCode`，但仍呼叫 `onComplete(false, sendEvent, response)`」——**`success` 依然是 `false`，但事件會送、response也會傳**。連帶把 `ExecuteSequential` 內部消費 callback 的邏輯從「包在 `if(success)` 裡才送事件/加response」改成「看 `sendEvent`/`response` 是否為null」，不再跟 `success` 綁死——這是必要的連動修正，否則例外碼場景即使 `HandleResponse` 傳了 `sendEvent` 也不會真的被呼叫
- 新增 `DRT_Const.SEASON_LEADERBOARD_STATUS_CODE_NO_SEASON_SCORE = 27301`（賽季尚無任何積分），只有 `RequestSeasonLeaderboard` 這個call site傳這個例外碼，其餘6個API呼叫不受影響（行為不變）
- **這是「業務層級非致命錯誤碼」的處理先例**：之後其他API若也遇到「Server回傳的某個特定StatusCode不算真正錯誤、但也不是success、且consumer需要讀取這個response內容」的情境，可以比照這個模式在對應的 `RequestXxx` 呼叫加上專屬例外碼，不需要再改一次 `HandleResponse`/`ExecuteSequential` 本體
- `DRT_SeasonStateManager` 監聽 `SeasonLeaderboardResponse`：`retstatus.StatusCode==27301` → `isSeasonOngoing=false`；否則(success) 用 `seasonTimeManager.GetSeasonPeriodTimeInfo()`+`timeManager.GetCurrentTime` 判斷是否在區間內
- `ActivityStateInitComplete` 從「直接發送SeasonStateUpdateComplete」改成「先RequestApi(SeasonLeaderboard)，callback後才發SeasonStateUpdateComplete」——不檢查傳入的 `ActivityState` 參數值
- `UpdateSeasonState` 三分支（新增 `IRandomDelayProvider` 依賴 + `PrepareOpenActivityPage` 取消機制，`prepareOpenCts`）：①頁面開啟(不論賽季狀態)→立即RequestApi(無延遲)；②原尚未開始+頁面未開啟→隨機1~10秒延遲(可取消)→RequestApi；③原進行中+頁面未開啟→固定1秒延遲(沿用`REQUEST_UPDATE_SEASON_STATE_DELAY_SECONDS`)→直接送SeasonStateUpdateComplete，**不打API**——這三條互斥分支要分清楚，尤其③是唯一不呼叫RequestApi的情境
- 新增 `DRT_Const.REQUEST_UPDATE_SEASON_STATE_MIN_DELAY_SECONDS`(1)/`MAX_DELAY_SECONDS`(10)，跟既有的 `REQUEST_UPDATE_SEASON_STATE_DELAY_SECONDS`(固定1秒，給③用)是不同的const，不要搞混
- 測試設計：`GivenSeasonOngoing(bool)` helper 內部透過**觸發 `SeasonLeaderboardResponse` 事件**（而非直接設欄位）來準備「原賽季狀態」前置條件，因為 `isSeasonOngoing` 欄位只能透過這個事件流程被設定，這是配合新設計的必然寫法
- **⚠️ 換手案例與測試本體不一致的處理案例**：`receive_activity_state_init_complete_...` 這個既有測試方法，使用者只改了註解(改成新流程敘述)但忘記同步改測試本體斷言，經確認後才依新註解重寫斷言+改方法名稱——**之後遇到"註解已更新但測試本體斷言還是舊的"這種不一致，要主動指出來問清楚，不要照舊斷言硬套新註解，也不要照新註解硬猜斷言**
- 也順便幫 `DRT_ApiRequestHandlerTest.cs` 新增3個測試涵蓋這次例外碼異動（成功走例外路徑、`GetResponse<T>()`仍可取得response、其他API即使StatusCode恰好相同也不受影響仍走原本error handle）——這是額外主動補的，因為改動到共用基礎設施理應要有對應覆蓋，不算範圍外
- **提交流程確認**：submodule (`ThirdParty`) 新增 SubEventId 這類commit，即使不屬於`aiToolLib`，也要遵循`[前綴][系統][次要功能]`格式（例如`[feat] [分組日榜] 新增賽季狀態相關事件：...`），第一次commit漏加`[feat]`前綴後有用`git commit --amend`修正（因為SHA尚未在主專案引用，屬於低風險的自我修正，不算「已發布」的commit）
- 現在 29 個模塊中 **21 個已完成**，7 個待處理：`DRT_SeasonStateManager`（下一個，已有預寫換手草稿：初始化監聽事件、依當前時間是否在賽季起訖時間內判斷「進行中/尚未開始」、收到"活動狀態初始化完成"事件後的處理，但最後一條草稿comment不完整——`收到"活動狀態初始化完成"事件後, 若當前時間未在賽季起訖時間內, ` 後面被截斷，下次要先請使用者補完整）、`DRT_SeasonRankingManager`、`DRT_FirstOpenTrigger`、`DRT_ToastPopupManager`、`DRT_ToastPopupQueueHandler`、`DRT_SeasonPageViewPresenter`、`DRT_ToastPopupViewPresenter`

**2026-08-04 新增 `DRT_SeasonSettings.GetSeasonSpecialReward()`**（commit `ca35e75ebdd`）：讀取 `CommonVariable.IPConfig.DailyRankTournamentSeasonSpecialReward`（pipe-delimited格式 `"{特殊獎勵字串}|{RankFrom}|{RankTo}"`）解析成 `DRT_RankReward`（新增 `specialRewardKey` 欄位）。509個測試全過。
- **⚠️ `CommonVariable.IPConfig` 裡類似設定一律是 `public static string`，不是 `const string`**：使用者一開始口頭說「const字串」，但這個檔案裡所有同類 pipe-delimited 設定（`MultipleHall_VIP_BetCount`、`PointLuckyDrawNotificationSetting`等）都是 `static`，因為 `const` 在編譯期就固定死，測試沒辦法讓它在執行期變成不同的值來驗證「無資料/格式錯誤/正確」這幾種情境——已跟使用者確認過改用 `static`，之後遇到類似「使用者說要const但這是要給測試覆蓋不同情境的設定值」的情況，要主動提出這個技術衝突，不要照字面實作
- 測試對這種全域靜態欄位的操作模式：`[SetUp]` 存下原始值到私有欄位、`[TearDown]` 還原，避免污染其他測試（這是操作 `CommonVariable.IPConfig` 這類全域可變狀態時的標準做法，之後遇到類似情境直接比照）
- 預設值訂為空字串 `""`（使用者確認），不是換手案例裡的範例值 `"SpecialReward|1|5"`（那只是拿來說明格式的例子）

**通用踩雷紀錄（適用後續模塊）：**
- 時間戳一律用 `TimeManager.ConvertDateString(dateString)` 轉換，不要手動用 `DateTimeOffset` 運算（專案時間戳單位是**毫秒**）
- 事件系統走 `YileEventTools/YileEventManager`（`EventIdEnum.DailyRankTournament` + `DailyRankTournamentSubEventId`，定義在 submodule `Assets/ThirdParty/Libs/Event/`）
- `DailyRankTournamentModel.cs` 採用「`CreateModuleInstances()` 純建構 + `InitializeModules()` 呼叫每個模塊 `Initialize(...)` 注入依賴」兩階段模式
- asmdef：`Yile.Lobby.Activity.DailyRankTournament.asmdef` 要 reference `Yile.ThirdParty.Libs.Event`；新模塊的測試 asmdef GUID 要加進 `Yile.Lobby.Editor.asmdef`（不是 `Yile.Lobby.asmdef`，加錯會循環參照）
- Unity MCP `manage_asset`（rename/move）的 `success:false` 不可靠，每次呼叫後要用檔案系統重新確認實際結果
- submodule（`ThirdParty`、`aiToolLib`）都在正常 `develop` branch 上，非 detached HEAD
- Response/Request 等組合型資料結構的測試 `Create` 方法要開參數＋預設值，不要把所有欄位寫死在無參數方法裡（已補進 `rules/14-unit-test.md`）
- `ResInfo.type` 測試資料一律用 `nameof(GenericItemDefine.Xxx)`，不要自己編字串（已補進 `rules/14-unit-test.md`）
- **編譯確認只在最後一次做**（跟 `run_tests` 一起），中途不需要頻繁檢查編譯
- commit message 若涉及 submodule pointer 更新，且該次 submodule 內容跟目前功能無關（例如純規範文件更新），標籤要用 submodule 本身（如 `[aiToolLib]`），不要硬套目前功能名稱

**⚠️ 2026-07-17 起：`activity-iterate-module` Step 4（更新架構文件）新增 2 條規範**：
- 若實作後需要改寫「用途概述」，要先跟使用者確認預計改成的新內容，不能直接覆蓋
- 「Public Function」清單不列 `Initialize(...)`（每個模塊都有、屬串接機制本身，不是對外功能）——既有的 `DRT_QualificationTierSettings`/`DRT_PlayerQualificationManager` 文件已依此規則移除 Initialize 條目

**⚠️ 2026-07-17 起：`activity-decompose-module` 與 `activity-iterate-module` 已合併重構，流程整個改了**：
- 拆解模塊現在只剩 Step 1-6（蒐集敘述、比對規格、共用模塊、建空殼、列架構文件初稿）+ Step 7（**迴圈呼叫「迭代模塊」**逐一處理每個「是否為待處理=是」的模塊）+ Step 8（最終驗證與回報）
- 單一模塊「怎麼處理」的完整邏輯（換手→實作→ViewPresenter檢查→串接進Model→更新架構文件）**全部搬到 `activity-iterate-module` 的 Step 2 分支A ~ Step 4**，拆解模塊自己不再重複這份邏輯
- 迭代模塊現在有「兩種進入方式」：獨立觸發（使用者直接喊「迭代模塊」）走完整偵測；被拆解模塊迴圈呼叫時，跳過偵測直接進分支A的換手/實作，且跳過它自己 Step 5 的驗證（測試統一留給拆解模塊 Step 8 做一次）
- 分支判斷邏輯也改了：不再是單純看「檔案存不存在」，而是看「class 本體是否仍是空殼」（對照架構文件「是否為待處理」欄位），避免把拆解模塊剛建好的空殼誤判成「已存在→直接修改」而跳過換手

**⚠️ 2026-07-17 起：`activity-iterate-module` 分支A新增「實作前檢查」步驟**（換手收到訊號後、正式實作前）：
1. 測試案例的功能是否符合架構文件對該模塊的定義（避免像上面 GetMatchStartTime 那樣把別的模塊該管的功能誤植進來）
2. 是否需要串接 Server API 資料結構，若有先擬串接欄位對應方案跟使用者確認
3. 是否有案例可合併成 `[TestCase]` 參數化測試
三項都需跟使用者確認過才開始實作。之後處理 `DRT_MatchTimeManager`、`DRT_QualificationTimeManager` 等剩餘模塊時，這個檢查會是換手後的必經步驟。

**2026-08-04 session結束時現況快照（供下個session接手參考）：**
- 目前 commit HEAD 是 `cef2954a881`，所有本次session的異動都已commit（不含push，符合[[feedback_no_auto_push_except_gas_url]]規則）
- `git status` 顯示以下**非本次session造成、使用者自己在IDE端進行中的未commit異動**，跟模塊拆解無關，維持不動不要清除：`DRT_SeasonPanel.cs`/`DRT_ISeasonPanel.cs`/`DRT_SeasonRewardBanner.cs`/`DRT_SeasonRewardBannerDisplayUnit.cs`（賽季頁 View 層，疑似parse-ui-mockup相關工作）、`DRT_SeasonPageViewPresenterTest.cs`（下下個模塊的預寫換手草稿）、Server API文件 `_分組日榜 DailyRankTournament.xlsx`（疑似欄位更新）
- 29個模塊中 **24 個已完成**，**5 個待處理**：`DRT_FirstOpenTrigger`（下一個，架構文件順序）、`DRT_ToastPopupManager`、`DRT_ToastPopupQueueHandler`、`DRT_SeasonPageViewPresenter`、`DRT_ToastPopupViewPresenter`
- `DRT_FirstOpenTrigger`/`DRT_ToastPopupManager` 這兩個模塊的測試檔案目前狀態未知（可能是空殼待換手），下個session開始前建議先讀 `DRT_FirstOpenTriggerTest.cs` 確認是否已有換手案例

**How to apply：** 新 session 若被要求「繼續拆解分組日榜模塊」或「分組日榜下一個模塊」，**先跑 `git status`/`git diff` 確認有沒有未 commit 的異動**（曾發生欄位宣告被註解掉但用法沒清的破壞編譯狀態，需先跟使用者確認怎麼處理才能繼續），再讀取架構文件確認哪些模塊「是否為待處理」仍是「是」，依目前版本的 `Skills/Lobby/ActivityDev/activity-decompose-module/SKILL.md` Step 7（迴圈呼叫迭代模塊）繼續，實際處理單一模塊時去讀 `activity-iterate-module/SKILL.md`（記得換手後先走「實作前檢查」三項），並沿用上述架構決策與命名鐵律，不需重新確認。`ExtensionResponse.DRT_MemberBetUpdate` 這個通靈占位WS事件已於2026-08-10整條移除（含`DRT_MemberBetUpdateResponse`資料結構、`DRT_PlayerQualificationManager.OnReceiveBetUpdate`、`DailyRankTournamentSubEventId.MemberBetUpdate`/`MemberBetAndLevelUpdate`），之後若玩家押碼量有真正的Server WS推播需求，需要重新設計，不要以為這裡還有舊流程可以參考沿用。

**2026-07-26 完成 `DRT_MatchPageViewPresenter`**（commit `585aaa22bf7`）：50個測試全過，串接進 `DailyRankTournamentModel`（依賴 `IYileEventAdapter`/`IYileEventTools`/`ITimeManager`/`IActivityPeriod`/`DRT_IMatchStateManager`/`DRT_IMatchTimeManager`/`DRT_IMatchRankingManager`/`DRT_IMatchLeaderboardDropdownParser`/`DRT_IMatchSettings`/`IResInfoParserModel`）。
- **關鍵設計**：初始化畫面流程與下拉選單切換共用同一個「選單項目顯示流程」內部function（新增 `DRT_IMatchPageViewPresenter.OnMatchDateSelected(int index)`，`DRT_MatchPanel.OnMatchDateDropdownValueChanged` 轉發呼叫這個，不透過onValueChanged本身觸發初始化流程）
- 「今日無賽事」判斷不看選項顯示文字，直接看 `DRT_MatchLeaderboardDropdownOption.matchId` 是否為空字串（`DRT_MatchLeaderboardDropdownParser` 建立選項時就是這樣區分的）；「今日榜單」判斷則是 `option.matchId == matchStateManager.GetCurrentMatchId()`（且 `HasOngoingMatch()==true`）
- 新增 `DRT_IMatchLeaderboardDropdownParser` 介面（純轉出 `GetDropdownOptions()`），`DRT_IMatchRankingManager` 補上 `RequestMatchLeaderboard`/`GetCurrentMemberLeaderboard`（原本只有 concrete class 有，這次變成消費端真正需要才補介面）
- **⚠️ `.CentToCredit()` 踩雷**：這個共用擴充方法（`Assets/ThirdParty/SCG_Script/CustomStringExtension.cs`）固定用 `"{0:N0}"` 格式化，**不會顯示小數**，跟這次「贏分文字永遠2位小數（0.00/1,000.00）」的需求不符，改用 `(score / 100m).ToString("N2", CultureInfo.InvariantCulture)` 自己格式化。之後任何需要「永遠固定小數位數」的金額顯示需求，**不要預設沿用 `.CentToCredit()`**，先確認這個共用方法的實際格式規則是否符合需求
- `DRT_MatchPanel`/`DRT_IMatchPanel` 新增：活動結束面板、無賽事提示、排行榜面板 show/hide；`ShowTopRewardCardImage` 改為多帶一個 `string text` 參數；`SetMatchDateDropdownLock(bool)` 除了原本的 `interactable` 外也真的 `SetActive` 整個下拉選單 GameObject（隱藏/顯示）
- `DRT_MatchRankInfiniteCell` 拆出 `public void ShowRankData(DRT_RankScrollDisplayUnit data)`，`OnUpdate()` 與 `DRT_MatchPanel.ShowSelfRank()` 共用同一份轉換邏輯，不重複寫
- `DRT_ActivityPeriodManager.cs` 的除錯假資料異動（2026-07-25記錄的那份）**維持不動、沒有commit進這次的commit裡**，跟這次功能無關
- **⚠️ 2026-07-26 後續修正（commit `ffd5ac8f0d5`）**：使用者發現「本場剩餘」「賽事已結束」「賽號」這三個顯示文字被我直接寫死在 `DRT_Const.cs` 裡，應該要走字表——已改為在 `Tools/Math_Data/~Localization/daily_rank_tournament_ui_localize.xlsx` 新增3個Key（`DAILY_RANK_TOURNAMENT_MATCH_STATUS_REMAINING`/`_ENDED`/`_MATCH_NUMBER_PREFIX`），`DRT_Const.cs` 改存 `LOCALIZE_KEY_...` 常數，Presenter補上 `IDataTableManager` 依賴透過 `GetText(key)` 取得文字，並執行過 `Convert All xlsx to Localization` 選單。**之後任何模塊只要有 View 顯示中文字的需求，都要主動想到走字表，不要預設寫死字串**——這條規則已經寫進 `activity-iterate-module/SKILL.md` 的通用規則區塊（`D:\Git\lobby-agent-client` commit `8b65103`），之後不用每次都重新提醒
- 現在 25 個模塊中 **17 個已完成**，依架構文件列表順序下一個是 **`DRT_SeasonSettings`**（賽季設定），目前仍是純空殼，尚未換手

**2026-07-28 完成 `DRT_ActivityInfoPageViewPresenter`**（活動說明頁面呈現器，非依架構文件列表順序，使用者直接跳來處理）：23 個測試全過，對應 View 為 `DRT_HelpPanel`（4 個內部頁籤：活動說明/賽事階級/賽事獎勵/賽季獎勵）。串接進 `DailyRankTournamentModel`，並補上 `DailyRankTournamentView.helpPanel` 的 BindPresenter 流程（連帶 `DRT_DailyRankTournamentViewPresenter.Initialize`/`OpenAsync<DailyRankTournamentView>` 都多帶一個 `DRT_IActivityInfoPageViewPresenter` 參數，`DRT_DailyRankTournamentViewPresenterTest` 也同步更新）。
- **⚠️ 重要澄清：「是否為待處理」不是純粹看程式碼是否為空殼**——這次為了讓 `DRT_ActivityInfoPageViewPresenter` 能編譯串接，順手在 `DRT_SeasonSettings`（新增 `GetSeasonRewards()`，監聽 `RewardInfoResponse`）、`DRT_ToastPopupManager`（新增 `IsToastPopupOn()`/`SetToastPopupOn()`，PP 持久化+發送事件）補了「最小實作」，兩者都已經不是空類別、有真實邏輯，但使用者明確指示**這兩個模塊本身都還沒真正開始實作，維持「是否為待處理：是」不變**——之後這兩個模塊真正輪到時，仍要走完整的「換手→實作前檢查→實作」流程，不能因為架構文件目前 Public Function 有內容就誤判成已完成。`DRT_SeasonTimeManager` 也補了 `DRT_ISeasonTimeManager.GetSeasonPeriodText()` 但內容純粹 `return string.Empty`（純占位，Server 尚無賽季起訖時間欄位），同樣維持待處理
- 新增 SubEventId `ToastPopupOnStatusChanged`（submodule `ThirdParty`，「通知彈窗開啟狀態變更」，帶 bool 參數）
- 名次區間顯示格式（`DRT_RankReward.rankFrom/rankTo` → 表格「名次」文字）：`rankFrom==rankTo` 顯示 `"{rankFrom}"`；`rankTo==-1` 顯示 `"{rankFrom}之後"`（不展開成多列）；其餘顯示 `"{rankFrom}~{rankTo}"`——這是使用者中途修改過測試案例定案的版本（原本討論過範圍展開成多列，最後否決，改回單列顯示區間文字）
- 賽事獎勵階級下拉選單名稱直接取 `DRT_IQualificationTierSettings.GetLevelSettings()` 的 `levelName`，由高到低排序（`OrderByDescending(level)`），預設選最高階級（index 0）
- **⚠️ 發現一個跟這次任務無關的既有 bug**：`Assets/Scripts/Lobby/Activity/DailyRankTournament/Models/DailyRankTournamentDummyDataCreator.cs:14` 少一個分號，這個編譯錯誤已經在 HEAD（commit `2dc3dcc836c`）裡，導致整個專案先前都無法編譯/跑測試——已直接補上分號修掉（純語法修正，無邏輯變更）。修完後跑全部 `SCGLobby.Tests.DailyRankTournament` 測試群組（426條）發現 **`DRT_QualificationPageViewPresenterTest` 有 11 條既有測試失敗**（`GetQualificationStatus`/`GetLevelRankReward` 相關的 NullReferenceException，跟這次改動的檔案完全無關）——**已於同一輪修復**（commit `06506f246ef`），只動測試檔沒動 interface，詳見下方 NSubstitute 踩雷提醒的第二點
- **⚠️ NSubstitute 踩雷提醒（續，比第一點更隱蔽）**：debug 這 11 條失敗時，用 `read_console`/`get_test_job` 都拿不到 NUnit 失敗的 stack trace（Unity Test Runner 的例外訊息只有 `message`，沒有 stack trace 欄位；`Debug.Log` breadcrumb 在測試執行期間也不會出現在 `read_console`），最後改用「刻意 `throw new Exception($"...")` 印出關鍵變數值」的方式才問出真相：`playerQualificationManager.GetQualificationStatus()`（介面方法，回傳型別 `DRT_QualificationStatus`，純 class 只有 public 欄位無自訂建構子）在**完全沒設定 `.Returns(...)` 的情況下，NSubstitute 竟然回傳非 null 的空欄位物件**（`currentLevel=0` 等預設值），但同一個測試檔案裡 `matchSettings.GetLevelRankReward(...)`（回傳型別 `ResInfo`）在同樣未設定時卻正常回傳 `null`——兩者行為不一致，原因未完全查清（懷疑跟 NSubstitute 對「純欄位無建構子的 class」的某種 auto-value 行為有關，但沒有確認到底是版本特性還是其他原因）。**之後遇到「應該要 null 却没 NRE」或反過來「不該 null 却 NRE」的詭異情況，不要predict，直接暫時在 production code 插入 `throw new Exception($"變數={變數}")` 這種探針去問，比對照原始碼推理更快更準**，問完記得用 `git diff --stat` 確認 production code 已完全還原成探針插入前的狀態才能放心。這次的實際解法：`[SetUp]` 同時補上 `matchSettings.GetLevelRankReward(Arg.Any<int>(), Arg.Any<int>()).Returns(new ResInfo())` + `rewardInfoParser.GetRewardPackagedInfo(Arg.Any<ResInfo>()).Returns(new DRT_RewardPackagedInfo(string.Empty, null))` 兩層都補預設值，確保 `GetLevelRankRewardInfo` 不管走到哪一層都不會回傳 null
- **⚠️ NSubstitute 踩雷提醒**：mock 介面方法若未 `.Returns(...)` 設定，reference type 預設回傳 `null`，若 production code 假設該方法「永遠不會回傳 null」（例如 `DRT_IRewardInfoParser.GetRewardPackagedInfo` 的真實實作永遠回傳非 null 物件），測試沒補預設值就會在呼叫 `.RewardName` 時 NRE。解法是在 `[SetUp]` 補一個 catch-all 預設值（`rewardInfoParser.GetRewardPackagedInfo(Arg.Any<ResInfo>()).Returns(new DRT_RewardPackagedInfo(string.Empty, null))`），個別測試需要特定文字時再用具體參數覆蓋（NSubstitute 對「具體值」的設定優先於 `Arg.Any`，不用擔心覆蓋順序）

---

## 2026-08-06 session 結束快照（供下個 session 接手參考）

**目前 commit HEAD：`ccbb6ec9e01`**，本次 session 所有異動都已 commit（不含 push，符合 [[feedback_no_auto_push_except_gas_url]]）。29 個模塊中 **25 個已完成，4 個待處理**：`DRT_FirstOpenTrigger`（下一個，依架構文件順序）、`DRT_ToastPopupManager`、`DRT_ToastPopupQueueHandler`、`DRT_ToastPopupViewPresenter`。

**本次 session 完成的工作（依序）：**
1. **修正 `DRT_QualificationTimeManager.RequestMemberQualificationAsync` bug**（commit `92cd2dff51a`）：發送 `RequestApi` 事件時直接帶 `DRT_ApiType` enum，而非該事件實際需要的 `DRT_ApiRequestParams`，導致 `GetData<T>()` cast 失敗。修正後盤點全系統其餘 `RequestApi` 呼叫點，確認只有這一處型別不符
2. **修正 `DRT_QualificationPageViewPresenter` 押碼量單位錯誤**（commit `a7f6273f049`）：進度條押碼量顯示直接對 cent 數值套千分位格式，沒有除以100轉點數，改用 `.CentToCredit()`
3. **完整實作 `DRT_SeasonPageViewPresenter`**（commit `9caeb5223f9`，模塊拆解關鍵里程碑）：開啟頁面/主面板/倒數計時/獎勵Banner/排行榜卷軸/自己名次列刷新流程，新建 `DRT_SeasonSpecialRewardSO`/`DRT_ISeasonSpecialRewardSetting`（特殊獎勵SO設定），並補上 Model 裡原本漏掉的 `seasonPageViewPresenter.Initialize(...)` 串接。這是本次 session 最大的一塊工作，`DRT_SeasonPageViewPresenter` 因此從「是否為待處理：是」變成「否」
4. **賽季自己名次列改回沿用 `DRT_MatchRankInfiniteCell`**（commit `076bf7bbe05`）：`DRT_SeasonPanel.cell_selfRank` 欄位型別從我建的 `DRT_SeasonRankInfiniteCell` 改回使用者指定的 `DRT_MatchRankInfiniteCell`，`ShowSelfRank` 改成把 `DRT_SeasonRankScrollDisplayUnit` 轉換成 `DRT_RankScrollDisplayUnit` 再丟給共用 cell
5. **移除賽事頁已無用的「預設榜首獎」流程**（commit `966e4b88b50`）：`DRT_MatchPanel.go_defaultTopReward` 欄位、`ShowDefaultTopReward()`/`HideDefaultTopReward()`、Presenter 對應分支與測試全部刪除
6. **`DRT_SeasonRankInfiniteCell` 補上 `InitPlayerPicButton`**（commit `c8c1ccc37fd`）：比照 `DRT_MatchRankInfiniteCell` 補上點擊玩家頭像開啟資訊頁流程，新增 `DRT_ISeasonPageViewPresenter.OnClickPlayerPic`、`DRT_SeasonRankScrollDisplayUnit.Presenter` 欄位
7. **排行榜獎牌圖示改用共用SO設定**（commit `d4bd7086ab6`，方案B）：新建 `DRT_MedalIconSO`/`DRT_IMedalIconSetting`，`DRT_RankScrollDisplayUnit.RankMedalIndex`(int?)/`DRT_SeasonRankScrollDisplayUnit.RankIndex`(int?) 都合併簡化成單一 `RankMedalIcon`(Sprite) 欄位，由 Presenter 查表算好實際圖片後帶給 cell（cell 不再各自持有 `sprites_medalIcons` 陣列）。**`MedalIconSetting.asset` 已自動帶入原本兩份 prefab 共用的 3 張獎牌圖片**（沿用原 GUID，不用重新指定）
8. **補上獎勵氣泡框（rank reward tips）相關測試 + 修正既有流程**（commit `ccbb6ec9e01`）：這部分底層功能（`CommonTips`/`ScrollFollowMidlineDetector`/`DRT_MatchRankRewardInfoContent`/`OnClickRewardInfoButton`）是使用者自己並行開發完成的，我只負責補測試案例，順便修正兩處跟換手案例對不上的地方：①`InitializePageContent()`原本呼叫一般 `CloseRankRewardTips()`（會播動畫），改成新增的 `ForceCloseRankRewardTips()`（接 `CommonTips.ForceClose(false)` 不撥動畫）②`OnClickRewardInfoButton` 原本「無跟隨物件/無獎勵資料」時只是單純 return 不做事，改成真的呼叫 `CloseRankRewardTips()`（一般關閉，非強制）。`ClosePage()` 也補上強制關閉氣泡框+清空跟隨目標

**⚠️ 這次 session 中途曾遇到 Unity Editor 長時間完全無回應**（`refresh_unity`/`get_editor_state` 連續多次逾時、`read_console` 回「ping not answered」），持續數分鐘後靠使用者確認「已連上」才恢復（`refresh_unity` 回報 `recovered_from_disconnect`）——這比先前記錄過的「RPC逾時但背景還在跑」更嚴重，之後若再遇到連續多次(3次以上)逾時且完全ping不通，應該停止盲目重試、告知使用者請他確認Editor視窗狀態，而不是繼續乾等或猜測。

**⚠️ commit message 規範這次 session 有更新**（詳見 [[feedback_auto_commit_format]]）：內容盡量避免全英文的 function/class/const 識別字，改用中文描述「做了什麼」；但「真正的專案專有名詞」（例如 `CentToCredit` 這種團隊慣用的擴充方法名稱）可以直接寫英文，不用硬翻譯——判斷標準是「這個詞是不是專案裡大家已經在用的固定稱呼」。

**⚠️ 工作區持續有使用者自己在 IDE 端並行開發的異動，跟模塊拆解無關，維持不動不要清除**：材質檔（多個 `*LightSweep*.mat`，疑似 Unity 重新烘焙）、`Assets/SourceFiles/GameItem/GameIcon/Spine/...`（Spine 動態圖示相關）、`Assets/VContainer/RootLifetimeScope.prefab`、`Assets/mcs.rsp`、`Slot_Lobby.sln.DotSettings.user`、`docs/.../規格/DailyRankTournament_前端開發規格.md`、`docs/.../規格/分組日榜規格v2.md`。這些從 session 一開始就存在或是 Unity 背景重新序列化的產物，不屬於任何一次我的 commit 範圍。

**How to apply：** 新 session 若被要求「繼續拆解分組日榜模塊」，先跑 `git status`/`git log -5` 核對 HEAD 是否還是 `ccbb6ec9e01`（若不是，代表使用者自己後續又有進度，要重新核實現況再繼續），下一個要處理的模塊是 `DRT_FirstOpenTrigger`——依 `activity-decompose-module`/`activity-iterate-module` 流程，先確認 `DRT_FirstOpenTriggerTest.cs` 是否已有使用者換手好的測試案例註解，沒有的話要先請使用者換手才能開始實作。commit message 記得套用新規範（避免英文識別字，專有名詞例外）。

---

## 2026-08-10 session 結束快照（供下個 session 接手參考）

**目前 commit HEAD：`496bbd72e87`，branch `feature/Art_2607_DailyRankTournament`**（不是develop，這是這次通知彈窗系列工作所在的feature branch）。29 個模塊中 **27 個已完成，2 個待處理**：`DRT_FirstOpenTrigger`（`DRT_FirstOpenTriggerTest.cs` 仍是空殼，尚未換手）、`DRT_ToastPopupViewPresenter`（**已完整實作+測試通過+已commit**，見下方，不再是待處理）。

**同一天session恢復工作後追加完成（HEAD `f9db3eb9930e` → `496bbd72e87`，依序）：**
- commit掉原本未commit的 `DRT_ToastPopupViewPresenter` 實作（commit `b266ee237b5`），並更新架構文件該小節（commit `3eb21f8c6c1`，是否為待處理改為否、補依賴清單）
- **移除 `ExtensionResponse.DRT_MemberBetUpdate` 通靈占位WS事件整條流程**（commit `496bbd72e87`，submodule ThirdParty `93aee122`）：使用者確認這是當初尚未跟Server串接正式事件時通靈出來的，現已整條拔除，包含：`ExtensionResponse.DRT_MemberBetUpdate`常數、`DRT_MemberBetUpdateResponse`資料結構(整個檔案刪除)、`DRT_PlayerQualificationManager`的`webSocketMainClient`依賴與`OnReceiveBetUpdate`方法(連帶`yileEventTools`依賴也一併變成無用而移除)、`DailyRankTournamentSubEventId.MemberBetUpdate`/`MemberBetAndLevelUpdate`兩個SubEventId(submodule)、對應測試案例3條。**`GetTodayBet()`/`qualificationResponse.todayBet`保留未動**——這是`DRT_MemberQualificationResponse`(真實Server API)自帶的欄位，跟這次移除的通靈WS事件是兩條不同來源，移除後押碼量只能透過真實API輪詢更新，不再有WS即時推播管道
- 移除前後測試數量 641→638(少3條，剛好對應移除的3個測試方法)，全過，確認改動有真正編譯進去

**本次 session 完成並已commit的工作（依序）：**
1. `DRT_ToastPopupManager`（通知彈窗管理器）：監聽三個WS事件(明日賽事資格提升/達60%/今日賽事名次變化)，過濾非自己玩家/找不到階級資料/通知彈窗關閉/階級或名次無變動，發送"加入通知彈窗佇列"事件
2. `DRT_ToastPopupQueueHandler`（通知彈窗排隊管理器）：佇列分組替換(資格提升/達60%同組、名次變化自己一組)、依序彈出、播放完畢/切換場景後**延遲1幀(UniTask.DelayFrame(1))**才繼續判斷、新增"若在遊戲中但非老虎機或魚機類型遊戲則不彈出"判斷
3. `DRT_MainViewPresenter`：BindView/UnbindView新增螢幕轉橫向/直向事件(參考`BattlePassViewPresenter`)，新增`ICommonModel`依賴
4. **重要重構**：`DRT_MatchPageViewPresenter`/`DRT_SeasonPageViewPresenter` 原本各自複製一份「是否在魚機遊戲」判斷邏輯（重複3次以上），改成統一呼叫 **`ICommonModel.IsInFishGame()`**（專案裡魚機判斷唯一權威實作），移除不再需要的`IHallModel`依賴。**⚠️ 之後任何模塊要判斷「是否在魚機遊戲」，一律直接呼叫`commonModel.IsInFishGame()`，不要自己重寫`GameHallType`列舉比對**（`Fish`/`YileFish`/`AwakeFishing`/`BattleFishing`/`PiliFishing`/`ThreeKingdomsFishing`六個值的OR判斷）。「是否為老虎機」沒有對應的單一封裝方法，直接用 `hallType == GameHallType.Slot`（`GameHallType`本身沒有多個Slot變體，不像Fish有6個）
5. **commit格式教訓**（已寫入 [[feedback_auto_commit_format]]）：submodule 參照格式要用 `update {簡寫} to {短SHA}` 當內容裡的數字項目，不要自創`(submodule Name@SHA)`這種寫法；之後任何commit前都要重新讀那份檔案全文，不要憑印象或抄`git log`舊commit套格式

**⚠️ `DRT_ToastPopupViewPresenter` 已完整實作但尚未commit，下個session第一件事就是把它commit掉：**
- 新增檔案：`Models/DRT_ToastPopupViewPresenter.cs`、`Models/DRT_IToastPopupViewPresenter.cs`（**這個介面原本被使用者/工具誤建成`class`**，已修正成`interface`，`BindView`參數也從錯誤的具體類別`DRT_ToastPopupView`改回介面`DRT_IToastPopupView`）、`UnitTest/Editor/DailyRankTournament/DRT_ToastPopupViewPresenterTest.cs`（10個測試）
- `DailyRankTournamentModel.cs` 已加好 `toastPopupViewPresenter` 欄位/建構/`Initialize(yileEventAdapter, yileEventTools, viewsManager, sceneChangeHandler, commonModel)`呼叫
- 依賴：`IYileEventAdapter`、`IYileEventTools`、`IViewsManager`、`ISceneChangeHandler`、`ICommonModel`（**不需要**`IHallModel`，isSlotType用`!commonModel.IsInFishGame()`）
- 核心流程：收到"彈出通知彈窗"事件時，若`view`尚未綁定則`viewsManager.OpenAsync<DRT_ToastPopupView>(this)`（View的`OnOpened`會自己呼叫回`viewPresenter.BindView(this)`完成綁定，Presenter端`await`完就能直接用），然後依`popupType`轉呼叫`view.ShowXxxPopup(isSlotType, data)`；View一旦綁定過**不會再解綁**（沒有UnbindView，是常駐彈窗層，跟Icon那種動態開關不同）
- `OnPopupComplete()`（View動畫播完的callback）：清空彈窗、發送"通知彈窗播放完畢"事件、彈窗狀態設關閉
- 監聽"切換場景"(`ISceneChangeHandler.OnChangeScene`)跟"通知彈窗開啟狀態變更"事件：兩者都是「若彈窗正在顯示中則強制清空」的邏輯
- commit前記得：①依`feedback_auto_commit_format`規範重新讀一次格式檔案 ②`DailyRankTournamentModel.cs`裡如果IsInFishGame重構那批commit時有暫時抽掉又補回toastPopupViewPresenter那三行wiring，確認現在完整存在 ③架構文件`DRT_ToastPopupViewPresenter`小節Step4要補（是否為待處理→否、Public Function留空無對外查詢方法、依賴清單）

**⚠️ 工作區持續有不相關的既有髒檔案**（跟本次session任何commit都無關，維持不動）：`Assets/Scripts/Fish3D`（submodule untracked）、`ViewsManagerSetting.asset`、`DailyRankTournament_Icon_Diamond*.meta`、`GuildPopupMessageView.prefab`、字型SDF asset、`Slot_Lobby.sln.DotSettings.user`。

**2026-08-11 追加：`DRT_QualificationTierSettings` 新增 `GetSimpleLevelName(int level)`**（commit `d5d351c53fc`）：階級簡易名稱（去掉"級"字，如"菁英級"→"菁英"）改走本地化字表（`daily_rank_tournament_ui_localize.xlsx` 新增 `DAILY_RANK_TOURNAMENT_QUALIFICATION_LEVEL_SIMPLE_NAME_1~10`），**不是**從 `GetLevelName` 字串trim算出來——因為簡易名稱是固定10個階級名稱（新銳/菁英/豪傑/大師/宗師/榮耀/巔峰/霸主/史詩/神話），跟 Server 傳的 `levelName` 完全脫鉤，可獨立編修。新增 `IDataTableManager` 依賴（`Initialize` 多一個參數）。`DRT_ToastPopupQueueHandler` 的 `BuildQualLevelUpDisplayInfo`/`BuildQualOverSixtyPercentDisplayInfo` 改呼叫這個新方法（原本呼叫 `GetLevelName`，`DRT_ToastPopupManager` 內的階級存在性驗證維持用 `GetLevelName`，兩者用途不同不要混用）。**⚠️ 重要澄清（曾在動手前跟使用者確認過，避免猜錯方向）**：這類「從既有資料衍生出的簡化顯示文字」，若使用者要求走字表，通常代表想要一份跟 Server/既有資料脫鉤的獨立固定清單，而不是單純字串處理（trim/substring）；遇到類似模糊指示，先確認清楚再動手，避免建錯機制要重工。

**同一次追加**：也順便糾正了一個repo結構誤解，詳見 [[reference_808_lobby_repo_structure]]——`808_Lobby_6000` 本身就是主 repo，`Slot_Lobby` 沒有自己的 `.git`，`Tools/` 字表跟 `Slot_Lobby/Assets/` 程式碼其實是同一個repo，可以一起commit，不用當成跨repo操作。

**2026-08-11 追加：`DRT_ApiRequestParams.sendCompletionEventImmediately`(bool) 改成 `eventSendMode`(enum `DRT_ApiEventSendMode`)**（commit `f283e641733`）：三個值 `Immediate`(預設,立即發送)/`BatchOnFinish`(結束時一次回傳)/`None`(不發送)。`DRT_MatchRankingManager.RequestMatchLeaderboard` 明確設為 `None`（原本完全沒設定這個欄位，等於用預設立即發送，跟測試comment敘述的"設為不發送事件"對不上，這次補上讓程式碼跟comment一致）。`DRT_ApiRequestHandlerTest` 新增2個測試涵蓋None模式（不發送任何事件／callback與GetResponse<T>()仍正常運作，兩者互相獨立）。**這次是先給使用者看新測試案例的中文敘述確認過，才動手實作**（enum命名跟欄位改名由我決定，新測試案例讓使用者先審過）。

**2026-08-11 追加：`DRT_PlayerQualificationManager.GetQualificationStatus()` 押碼進度百分比改吃Server**（commit `392f33e9a48`）：`DRT_MemberQualificationResponse.progressPercent`（Server欄位，之前完全沒被用到）現在直接供給 `progressPercentage`（換算`/100f`，並用`Math.Min(1f,...)`限縮超過100的異常回應值），取代原本`CalculatePercentage()`自己拿progressValue/progressGoal算的邏輯（該私有方法已整個刪除）。**唯一例外**：處於最後一階時，`progressValue`/`progressGoal`不再用`todayBet-previousThreshold`算（押碼量超過門檻多少會讓比例超過100%，這是被修掉的舊bug），改成兩者直接寫死相等（=該階與上一階門檻差），`progressPercentage`也直接寫死`1f`完全不理會Server回應值——這是唯一一個"進度不吃Server百分比欄位"的分支，其餘分支都吃。測試改動：移除「倒數第二階」案例（跟「第一階」案例同一條middle分支公式，重複覆蓋)，「最後一階」案例改用`[TestCase(800)][TestCase(5000)]`驗證不論押碼量超過門檻多少都固定顯示滿；新增專門測試`progressPercent`轉換與限縮上限（含101→1f案例）；既有兩個測試（`not_reach_any_level`/`at_first_level`）移除連帶的百分比斷言（改由新測試專門覆蓋，避免職責重疊）。646個DailyRankTournament測試全過。**⚠️ 尚未處理的延伸點**：`DRT_QualificationPageViewPresenter`目前仍自己呼叫`CalculateProgressPercent(status.progressValue, status.progressGoal)`重算一次百分比（用ceiling+cap99+min1的顯示邏輯，跟這次改的算法不同），沒有改成直接讀`status.progressPercentage`——這是使用者原本提出這次需求的動機之一，但這輪只確認了Manager層，Presenter層還沒明確討論要不要動、要不要保留原本的cap99視覺效果，下次若使用者要繼續這個方向要先確認清楚才能動手。

**2026-08-11 追加：`DailyRankTournamentTester` 新增3個通知彈窗測試功能**（commit `c451d1923ad`）：資格提升(previousLevel/currentLevel可自訂)、資格進度(currentLevel/nextLevel可自訂,percent固定60)、賽事名次變動(previousRank/currentRank可自訂)。**採用真實WS推播模擬模式**（比照既有 `LianWinWebSocketTester` 的 `WebSocketMainClient.Instance.OnRecvMessage(fakeResponse)` + 手動組JSON entity字串），不是走YileEvent內部事件bypass——這樣會完整走過`DRT_ToastPopupManager`的WS event監聽/memberId比對/階級存在性檢查等真實流程，比直接發`AddToastPopupQueue`內部事件更貼近正式環境行為。`memberId`固定帶目前玩家真實ID（`UserDataManager.profile.memberInfo.memberID`），不開放自訂。**這類「模擬Server WS推播」的Tester，之後都可以照這個模式（`SendFakeWsResponse(command, entityJson)`）寫，不用另外設計新機制**。

**2026-08-11 追加：`DRT_MatchStateManager` 新增 `GetMatchStatus()`**（commit `2f0ea78b1b5`）：新增 `DRT_MatchStatus` enum(`None`/`Waiting`/`Ongoing`，英文enum member名+中文doc comment，不是把中文字當成member名本身——`Waiting`對應comment"配對中"、`Ongoing`對應comment"進行中"，這是延續整個codebase enum命名的既有慣例)。判斷依據：先看`HasOngoingMatch()`(來自`DRT_MemberStatusResponse.hasOngoingMatch`)，無進行中賽事直接回`None`；有的話再看`memberMatchResponse.matchStatus`字串("WAITING"/"ONGOING"，新增對應`DRT_Const.MATCH_STATUS_WAITING`/`MATCH_STATUS_ONGOING`常數)轉換，其他值(含未來可能的"ENDED"或null)一律fallback回`None`(這次沒被要求處理ENDED，不特地新增分支)。4個測試案例是使用者事先寫好的換手草稿(已存在檔案裡)，這次補上實作。650個DailyRankTournament測試全過。

**2026-08-11 追加：`DRT_QualificationPageViewPresenterTest` 補2個換手測試**（commit `7320354c492`）：驗證延遲刷新(`RefreshPageDelayed`, 收到"玩家參賽資格API回應"/"更新活動狀態"事件為Keep時觸發)期間頁面被關閉，1幀後不執行任何刷新動作——**這次純粹補測試，production code早就有`RefreshPage()`內的`if (!isPageOpen) return;` guard，不需要改任何production code**。測試手法：`CallEvent(...)`觸發`RefreshPageDelayed().Forget()`後、`await`前先同步呼叫`presenter.ClosePage()`（因為`Forget()`只是fire-and-forget，呼叫端不會被await卡住，繼續往下執行是同步的，這保證`ClosePage()`確實發生在`UniTask.DelayFrame(1)`的continuation跑之前），再`await UniTask.DelayFrame(2)`讓pending continuation真正執行，斷言view沒收到任何刷新方法呼叫。652個DailyRankTournament測試全過。

**2026-08-11 追加：`DRT_SeasonPageViewPresenter` 延遲刷新機制改用等待1幀**（commit `a04c00b2b70`）：`RefreshPageAfterDelayAsync`原本用`randomDelayProvider.DelayRandomSeconds(1,1)`(固定延遲1秒)，改成`await UniTask.DelayFrame(1)`，並補上延遲後的`if (!isPageOpen) return;` guard(原本完全沒有，若延遲期間頁面被關閉會照樣執行刷新，這是被補掉的隱性bug，跟`DRT_QualificationPageViewPresenter`/`DRT_MatchLeaderboardAutoRefresher`等已有這層保護的模塊看齊)。**改動起因**：使用者先在測試檔comment把「等待1秒」改成「等待1幀」(換手草稿)，但production code跟測試本體都還沒跟上——這正是[[dailyranktournament-decompose-module-progress]]反覆記錄過的「comment先改、本體要主動比對」模式。連帶影響：`randomDelayProvider`(`IRandomDelayProvider`)依賴、`DRT_Const.SEASON_PAGE_REFRESH_DELAY_SECONDS`常數都整個移除(改用DelayFrame後沒人用了)；測試檔原本靠`GivenDelayCompletesImmediately()`(mock讓DelayRandomSeconds同步完成)驗證的4個既有測試，改用真正的`async Task`+`await UniTask.DelayFrame(2)`等一次真實frame，這個helper也整個刪除。653個DailyRankTournament測試全過。

**2026-08-11 追加：`DRT_MainViewPresenter.UnbindView()` 改為關閉所有頁面**（commit `2569fdb7419`）：原本呼叫`ClosePreviousTab(currentTab)`只關閉目前顯示中的那個頁籤，改成呼叫既有的`CloseAllTabPages()`（跟`OpenActivityViewAsync`共用同一個方法，一次關qualification/match/season/activityInfoPage四個頁面）。`ClosePreviousTab`本身沒刪，`OnSwitchTab`切換頁籤時仍在用。**順便修掉測試輔助方法`GivenViewOpened`的既有缺漏**：漏了`activityInfoPageViewPresenter.ClearReceivedCalls()`（只清了qualification/match/season三個），這次新增的斷言第一次真正驗證到`activityInfoPageViewPresenter.ClosePage()`才踩到——之前所有測試都沒斷言過這個presenter的ClosePage次數，所以這個缺漏一直沒被抓到。653個DailyRankTournament測試全過。

**2026-08-11 追加：`DRT_RankReward` 特殊獎勵欄位改版**（commit `14afce7cdb8`，`DRT_RankReward.cs`本身的欄位異動是使用者自己commit`7661c6e9429`）：原本 `specialRewardKey`(string) 改成 `hasSpecialReward`(bool)，只出現在 `DRT_RewardInfoResponse.seasonRewards`（賽季榜獎勵，`levelRewards`賽事獎勵沒有特殊獎勵設計）。連動：
- `DRT_SeasonSettings.GetSeasonSpecialReward()` 改成從 `seasonRewards` 找 `hasSpecialReward==true` 的那一筆直接回傳（rankFrom/rankTo/rewards都是Server給的真實資料，不再是config字串現場拼出來的假物件）
- 新增 `GetSpecialRewardKey()`：直接回傳 `CommonVariable.IPConfig.DailyRankTournamentSeasonSpecialReward`（純字串passthrough），該設定值格式從原本pipe-delimited`"{key}|{RankFrom}|{RankTo}"`簡化成純粹是key本身，名次區間改由Server端資料判斷
- **這是「一個舊欄位拆成兩個獨立來源」的案例**：rankFrom/rankTo跟着`GetSeasonSpecialReward()`回傳的同一個物件走（不用另外查），reward內容用的key則完全獨立來自`GetSpecialRewardKey()`，兩者互不相關，消費端(`DRT_SeasonPageViewPresenter`)要同時判斷兩者皆非空才顯示
- `DRT_SeasonSettingsTest`：3個舊的「pipe-delimited格式驗證」測試整個作廢刪除，改成「有無資料被標記hasSpecialReward」的新測試；新增`GetSpecialRewardKey()`專屬測試
- 652個DailyRankTournament測試全過（653→652，淨減1個測試，符合案例增減預期）

**2026-08-11 追加：`DRT_MatchLeaderboardDropdownOption` 新增賽事狀態欄位**（commit `53c4ddb5634`）：新增 `DRT_MatchLeaderboardDropdownStatus` enum（`None`/`Waiting`配對中/`Ongoing`進行中/`History`歷史紀錄，跟 `DRT_MatchStatus` 是兩個獨立的enum，使用者明確要求不共用）。`DRT_MatchLeaderboardDropdownParser` 新增 `DRT_IMatchStateManager` 依賴：「今日賽事」選項的status依`matchStateManager.GetMatchStatus()`轉換(Waiting/Ongoing→對應值，其他→None)；「今日無賽事」選項固定`None`；歷史選項固定`History`。既有6個測試案例都補上明確的status斷言(避免新欄位變成沒人管的假陽性)。655個DailyRankTournament測試全過。**⚠️ 檔案裡還留著一條使用者預寫的換手草稿沒實作**：「//取得下拉選單列表資料, 若列表中有"今日無賽事"、"01/03 新銳級", 則驗證第一筆資料中的狀態為"None"」——這次使用者訊息只列了3個要處理的案例，這條沒被提到，故意保留沒動，下次要問清楚是否也要補上（雖然production邏輯已經支援，只是沒有專門測試覆蓋這個情境）。

**2026-08-11 事後簡化（commit `70f94bd17c1`）**：使用者指出既有6個測試案例（本來就在測optionName/matchId/level/matchDate內容）補的status斷言跟下方3個「專門驗證status」的新測試案例重複，要求拿掉——`AssertOption` helper跟6個呼叫點的status參數整個移除，恢復成原本4參數簽名。**這是本次「status欄位」任務的收斂教訓**：新增欄位時，若已經規劃要寫專門的status測試，其他「內容導向」的既有測試就不用順便補status斷言，避免重複覆蓋同一件事——之後遇到類似「新增欄位+新增專門測試」的組合，可以先問清楚要不要在舊測試裡也順便驗證，不要預設兩邊都要顧到。655個測試數量不變（只是刪减重複斷言，不影響案例數）。

**2026-08-11 追加：`DRT_MainViewPresenter` 暫停/恢復底層遊戲改成有條件執行**（commit `154d90512fd`）：原本`BindView`無條件`SuspendGame()`+跳"已幫您暫停旋轉"彈窗、`UnbindView`無條件`UnsuspendGame()`，改成用`!commonModel.IsEnableTransaction()`判斷「遊戲是否正在進行中」（`IsEnableTransaction()`本身的語意是「遊戲閒置、可以做交易」，回傳false代表遊戲正忙/正在旋轉），只有正在進行中才suspend+彈窗；新增`hasSuspendedGame`欄位記錄「這次BindView有沒有真的觸發suspend」，`UnbindView`只有這個欄位為true才呼叫`UnsuspendGame()`並清回false——避免「原本沒暫停卻硬要恢復」的不對稱行為。**彈窗訊息本身**是這次session前一輪先幫使用者查出來的既有共用文案`CommonLocalizeKey.COMMON_IN_GAME_TRANSACTION_DISABLE`（"已幫您暫停旋轉"），沿用既有key不用新建字表。**測試踩雷**：改完後`receive_update_activity_state_when_close_then_show_system_message_and_close_view`測試失敗，因為它透過`GivenViewOpened(...)`間接觸發`BindView`（預設mock情境下`IsEnableTransaction()`回傳預設值false→視為進行中→也會跳"已幫您暫停旋轉"彈窗），跟該測試原本要驗證的"活動已結束"彈窗混在一起變成2次呼叫——修法是幫`GivenViewOpened` helper補上`systemMessageModel.ClearReceivedCalls()`/`gameBridge.GameProvider.ClearReceivedCalls()`（這個helper先前只清了yileEventTools/viewsManager/view跟4個頁面Presenter，這次補齊，跟上次`activityInfoPageViewPresenter.ClearReceivedCalls()`漏掉的模式一樣，**這個測試檔的`GivenViewOpened`helper已經連續兩次因為漏清某個mock而在改動時炸開，之後每次改動這個Presenter都要留意這個helper是否又漏了新加的依賴**）。657個DailyRankTournament測試全過。

**How to apply：** 新 session 若被要求「繼續分組日榜」，先跑 `git status`/`git log -5` 核對 HEAD（若不是下方最新記錄的SHA代表使用者自己後續又有進度，要重新核實現況），確認工作區乾淨無漏commit後，直接進入下一個模塊 `DRT_FirstOpenTrigger`（需要先請使用者在`DRT_FirstOpenTriggerTest.cs`換手）。Unity MCP這個session有連線但偶發ping不通(2~3次)又自行恢復的情況，遇到時先重試1次、連續3次不通才需要請使用者確認Editor視窗狀態。

**2026-08-11 追加：`DRT_QualificationPageViewPresenter` 開始競賽時間顯示新增賽事前綴文字**（commit `88415d3c988`）：`RefreshMatchStartTimeSection()` 顯示格式從單純`"MM/dd HH:mm"`改成`"{前綴}MM/dd HH:mm"`，前綴文字（"對應賽事："）新增字表Key`DAILY_RANK_TOURNAMENT_QUALIFICATION_MATCH_START_TIME_PREFIX`透過`IDataTableManager.GetText()`取得，不寫死字串。29個模塊進度不變（這只是既有模塊`DRT_QualificationPageViewPresenter`的既有測試案例comment調整換手，不是新模塊）。
**2026-08-11 追加：`DRT_RewardInfoParser` 新增頭像框(AvatarFrame)獎勵支援**（commit `18569e7da79`）：`GetRewardPackagedInfo()`新增`GenericItemDefine.AvatarFrame`分支，圖片與文字**不是**照抄Coin/Gem的`GetResImage(resType,settingKey)`/`GetResName(resType,settingKey)`分開兩次呼叫模式，而是直接呼叫既有的`IResInfoParserModel.GetAvatarFrameImageAndNames(List<ResInfo>, settingKey)`（回傳`List<ImageContentPair>`，一次拿到`img`+`content`兩者）——這個方法內部對每筆resInfo做`param`轉`outlineId`再查`MemberPicOutlineModel.Instance.GetTitleById()`取得頭像框名稱，跟Coin/Gem的「同一大類共用一張固定圖示+固定名稱」語意不同，頭像框是「每個outlineId有自己專屬的圖片與名稱」。33個`DRT_RewardInfoParserTest`測試全過(32→33)，658個DailyRankTournament測試全過。
- **⚠️ 換手草稿字面「直接從ResInfoParserModel取得」是關鍵線索**：一開始差點照抄Coin/Gem模式用`GetResImage(GenericItemDefine.AvatarFrame, settingKey)`(type-based generic overload)，但這樣拿到的圖片會是頭像框大類的通用示意圖而非玩家實際要拿到的那張，且沒有對應的`GetResName(AvatarFrame,...)`可用——這個class本身並沒有註冊AvatarFrame的通用文字，只有`GetAvatarFrameImageAndNames`這個專門方法才支援。之後遇到"直接從XXXModel取得"這類敘述時，先grep該model介面裡有沒有對應型別的專屬方法（尤其是回傳"圖片+文字"組合物件的方法），不要預設套用其他資源類型已有的通用pattern。

**2026-08-11 事後改版：頭像框獎勵圖片改成固定圖(不再吃ResInfoParserModel回傳的圖片)**（commit `cef9cecf6c5`）：使用者要求圖片改從`DRT_ISeasonSpecialRewardSetting`(既有的賽季特殊獎勵設定SO，原本只有`GetReward(rewardKey)`)新增的`GetAvatarFrameImage()`欄位取得，文字仍維持呼叫`resInfoParserModel.GetAvatarFrameImageAndNames(...)`只取`content`（圖片欄位改為忽略不用）。連動：`DRT_SeasonSpecialRewardSO`新增`[SerializeField] private Sprite avatarFrameImage`；`DRT_RewardInfoParser.Initialize()`簽名新增`DRT_ISeasonSpecialRewardSetting seasonSpecialRewardSetting`參數(`DailyRankTournamentModel.cs`裡`CreateModuleInstances()`已經會建立這個依賴給`DRT_SeasonPageViewPresenter`用，順手傳給`rewardInfoParser.Initialize()`即可，不用額外載入)。658個DailyRankTournament測試全過(數量不變，純改既有測試斷言依賴)。
- **⚠️ commit時排除了`DRT_SeasonPageViewPresenterTest.cs`的一筆未commit異動**：使用者這次同時在IDE端對另一個檔案(`DRT_SeasonPageViewPresenterTest.cs`)的既有測試comment追加了一句「(若獎勵類型為頭像框, 則顯示固定圖片)」草稿(純comment，測試本體尚未跟進)，這是**另一個尚未輪到的獨立任務**(賽季頁的獎勵Banner顯示邏輯，不是這次動的`DRT_RewardInfoParser`)，維持不動未commit——這次commit只包含跟頭像框圖片來源改動直接相關的5個檔案。之後若使用者接著要處理`DRT_SeasonPageViewPresenter`那個Banner邏輯，要注意可能也要串接同一個`GetAvatarFrameImage()`欄位。
- **`SeasonSpecialRewardSetting.asset`本身尚未指定實際頭像框圖片**：新欄位目前是空值(null)，需要提醒使用者在Unity Inspector手動指定素材，這不是程式碼能代勞的部分。
- **⚠️ 執行「Convert All xlsx to Localization」選單這次觸發了預期外的大量無關檔案異動**：這個選單會一次轉換全部64個xlsx檔案，即使只新增了1個Key，`git status`卻多出`member_pic_outline_text`/`ui_localize`的Shared Data/zh-TW asset（純LF/CRLF換行符正規化，`git diff`實際無內容異動）以及`ResInfoParserSetting_DailyRankTournament.asset`（一個不相關的GUID參照被改掉，疑似Unity背景重新序列化）。**這次commit前逐一用`git diff --stat`/`git diff`核對每個待commit檔案的實際異動內容**，只挑出跟這次改動真正相關的7個檔案(`DRT_Const.cs`/`DRT_QualificationPageViewPresenter.cs`/`DRT_QualificationPageViewPresenterTest.cs`/xlsx本身/`daily_rank_tournament_localize`的Shared Data與zh-TW asset/`LocalizationTableMap.asset`)commit，其餘無關異動(含既有已知的PrefabRefGuard/Fish3D/材質/字型等背景雜訊)都排除不動——這是[[feedback_display_text_via_localization]]提過的`LocalizationTableMap.asset`必帶規則的延伸驗證，同時也印證了「Convert選單一次動全部字表檔案，commit前必須逐檔案核實diff內容」這個新增的通用提醒，之後任何走字表流程的模塊異動都要重複這個排查步驟，不能因為`git status`顯示modified就整批`git add`。

**2026-08-11 追加：`DRT_MatchTimeManager` 改用賽事狀態(`DRT_IMatchStateManager.GetMatchStatus()`)取代時間區間比對**（commit `fdc1da7185d`）：
- `GetCurrentMatchTimeInfo()`原本用`currentTime < matchStartTime || currentTime > matchEndTime`判斷是否在賽事區間內，改成直接讀`matchStateManager.GetMatchStatus()`，非`Waiting`/`Ongoing`才回null——這個方法內因此完全不再用到`timeManager.GetCurrentTime`（該欄位在class裡其他方法仍有用到，沒有變成無用）
- `OnMatchStateUpdateComplete()`原本只看`HasOngoingMatch()`二選一啟動"賽事開始"或"賽事結束"倒數計時器；改成`HasOngoingMatch() && GetMatchStatus()==Ongoing`才啟動"賽事結束"，其餘情況(無賽事、或有賽事但狀態為`Waiting`配對中)一律啟動"賽事開始"倒數計時器——新增1個測試案例(配對中→賽事開始倒數，3組跨日TestCase跟"無賽事"那組共用相同數值，因為都是同樣的`dailyMatchWindowStart`目標)，既有"有賽事→賽事結束倒數"測試補上`GivenMatchStatus(DRT_MatchStatus.Ongoing)`並改名為`..._and_ongoing_then_start_match_end_count_down`
- **⚠️ 共用測試helper連動遺漏點**：`GivenMatchEndCountDownArmed()`這個被4個"賽事結束倒數計時器時間到"系列測試(`match_end_count_down_time_out_*`)共用的helper，只設了`HasOngoingMatch(true)`沒設matchStatus——NSubstitute對enum預設回傳0(`DRT_MatchStatus.None`)，新production邏輯下會導致這個helper實際武裝到的是"賽事開始"倒數計時器而非"賽事結束"，讓那4個測試的`countDownCallback`指向錯的方法卻可能因為兩者都呼叫同一個`OnMatchCountDownTimeOut()`私有方法而表面上仍然"通過"（測試斷言的是事件是否送出，不是倒數計時器key本身）——已補上`GivenMatchStatus(DRT_MatchStatus.Ongoing)`。**這是本次修改「任何一個判斷分支從單一bool改成多狀態」時的通用提醒**：要地毯式找出所有共用的`Given...Armed()`類型helper，逐一確認它們預期武裝的是哪個分支，不能只看新增/修改的那幾個測試案例本身。
- 662個DailyRankTournament測試全過(658→662)。

**2026-08-11 追加：`DRT_MatchPageViewPresenter` 全面改用選單項目狀態欄位取代賽事ID比對**（commit `f223ba9641e`）：這是一次較大範圍的重構，把原本靠`matchId`是否為空字串（`IsTodayNoMatch`）、`matchId`是否等於`matchStateManager.GetCurrentMatchId()`（`IsCurrentMatch`）這兩個間接判斷方式，全部改成直接讀`DRT_MatchLeaderboardDropdownOption.status`（`DRT_MatchLeaderboardDropdownStatus`：`None`/`Waiting`/`Ongoing`/`History`）。
- **`DRT_IMatchStateManager`依賴整個移除**：改用status判斷後，這個presenter完全不再需要`matchStateManager`，`Initialize()`簽名少一個參數，`DailyRankTournamentModel.cs`呼叫處同步移除——**這是本次task使用者沒有明確要求、但因為依賴變成完全無用而主動清理的案例**，之後遇到類似「改用其他判斷方式後某個依賴變成完全沒人呼叫」的情況，這個專案的既有慣例是直接拔掉該依賴（有多次前例：`DRT_SeasonTimeManager`拔`IActivityPeriod`、`DRT_MatchPageViewPresenter`/`DRT_SeasonPageViewPresenter`拔`IHallModel`），不是留著讓它變殭屍依賴
- **新設計四狀態的分工**：`RefreshLeaderboardScrollSection`用switch處理全部4態（`None`/`Waiting`→顯示無賽事提示面板(`SetNoMatchHintText`+`ShowNoMatchHint`+`HideLeaderboardPanel`)、`Ongoing`→讀`matchRankingManager.GetCurrentLeaderboard()`、`History`→呼叫`RequestMatchLeaderboard`）；其餘4個區塊（倒數計時/賽號/階級標籤/新增的開始競賽時間/榜首獎）共用同一個`IsTodayNoMatchOrWaiting()`守衛（`None`或`Waiting`一律跳過整個區塊，不呼叫任何View方法），`Ongoing`/`History`兩態的差異則各自區塊內部再分岔判斷
- **無賽事提示面板文字改為動態**：`SetNoMatchHintText(string)`(View新介面方法，使用者已預先在`DRT_IMatchPanel`/`DRT_MatchPanel`建好)——`None`用字表Key`DAILY_RANK_TOURNAMENT_MATCH_NO_MATCH_HINT_TODAY_NO_MATCH`(今日沒有可以參與的賽事，<br>請查看資格頁，確認詳細。)；`Waiting`用`DAILY_RANK_TOURNAMENT_MATCH_NO_MATCH_HINT_WAITING`(配對您的對手中，<br>賽事將於{0}開始，`{0}`直接帶入`matchTimeManager.GetMatchStartTimeString()`原始回傳值，不額外格式化)
- **新增「開始競賽時間區塊」**(`RefreshMatchStartTimeSection`，View介面方法`ShowMatchStartTime`/`HideMatchStartTime`同樣是使用者已預先建好但production code還沒接的方法)：前綴字表Key`DAILY_RANK_TOURNAMENT_MATCH_START_TIME_PREFIX`(開始競賽：)；`Ongoing`用`matchTimeManager.GetCurrentMatchTimeInfo().startTime`轉`TimeManager.ConvertToDataTime(...).ToString("MM/dd HH:mm")`；`History`用`option.matchDate`("MM/dd"格式)+`DateTime.ParseExact(matchTimeManager.GetMatchStartTimeString(), "HH:mm:ss", ...).ToString("HH:mm")`組合成"MM/dd HH:mm"——**這是`DRT_QualificationPageViewPresenter`那個「對應賽事：」前綴字表Key的姊妹案例，命名故意不共用同一個Key**（`LOCALIZE_KEY_QUALIFICATION_MATCH_START_TIME_PREFIX`跟`LOCALIZE_KEY_MATCH_START_TIME_PREFIX`是兩個獨立常數，前綴文字內容也不同，"對應賽事："跟"開始競賽："），之後若有第三個頁面也要顯示賽事開始時間，記得核對是否該共用既有Key還是也要開新的
- **⚠️ 換手草稿字面「若選單項目狀態為進行中或歷史紀錄」/「若選單項目狀態為None或配對中」這類多狀態條件敘述，這次使用者主動要求全部改成`[TestCase(狀態A)][TestCase(狀態B)]`參數化測試**，不維持單一情境測試——之後遇到同樣「用『或』字連接多個狀態」的換手敘述，預設就採用這個參數化模式，不用再特地確認
- **View層已由使用者事先鋪好但production code尚未接上的方法，是本次任務的重要前置線索**：`SetNoMatchHintText`/`ShowMatchStartTime`/`HideMatchStartTime`在`DRT_IMatchPanel`/`DRT_MatchPanel`裡已存在（其中`ShowMatchStartTime`/`HideMatchStartTime`本來是給`DRT_QualificationPageViewPresenter`那邊沿用的介面命名，這次在`DRT_MatchPanel`也建了一份同名但獨立的實作），只是完全沒被任何production code呼叫過——**這類「介面方法已存在但零呼叫」的情況，通常代表使用者已經預先把View層準備好等我方接上，之後恢復工作或换手時看到View層有陌生的未使用方法，要主動懷疑是不是這次任務要接的東西，先grep確認呼叫處數量再判斷**
- 676個DailyRankTournament測試全過(662→676，淨增14筆，對應本次新增/參數化展開的測試案例)
- 架構文件`DRT_MatchPageViewPresenter`小節的「依賴清單」已同步移除`DRT_IMatchStateManager`；「用途概述」暫時沒改(沒有主動詢問使用者是否要把「開始競賽時間」補進描述，之後若使用者想更新這段文字需要先問過再改)

---

## 2026-08-13 session 結束快照（供下個 session 接手參考）

**目前 commit HEAD：`e77a7166625`，branch `feature/Art_2607_DailyRankTournament`**。29個模塊中 **28 個已完成**，只剩 **`DRT_FirstOpenTrigger`** 待處理（`DRT_FirstOpenTriggerTest.cs` 仍是純空殼，尚未換手）。這次 session 不是走「拆解模塊」逐一補新模塊，而是回頭對多個已完成模塊做行為細節迭代（用戶直接點名模塊+換手案例），依序完成並各自commit（皆已commit，不含push）：

1. **修Issue `LOBBYTW-5005`**（commit `93a9f185e93`）：資格頁在活動進行中但無下一場賽事時（例如賽季最後一天），`DRT_QualificationPageViewPresenter.RefreshMatchStartTimeSection()`原本沒有防呆，會把`matchTimeManager.GetNextMatchStartTime()`回傳的`0`（無效值sentinel）直接格式化顯示成「01/01 08:00」（Unix epoch UTC+8）——修法是`nextMatchStartTime<=0`時改呼叫`HideMatchStartTime()`。單號已轉「下一版修正」。
2. **`DRT_SeasonTimeManager.GetSeasonPeriodTimeInfo()`**（commit `17a4af1fe1c`）：`endTime`改讀新欄位`tournamentInfo.activityEndTime`（不是`lastMatchEndTime`），該欄位目前**Server API文件尚未登記**（只有口頭確認），測試同步移除`lastMatchEndTime`mock。
3. **`DRT_SeasonPageViewPresenter`倒數計時器**（commit `a6924a0687c`）：`RefreshCountDownSection()`的`NotStarted`分支從直接return改成併入`Ongoing`分支一起顯示倒數（`case NotStarted: case Ongoing:`），測試改用`[TestCase(NotStarted)][TestCase(Ongoing)]`參數化驗證同一行為。
4. **`DRT_MatchRankingManager`兩輪調整**：①（commit `b028f23c31d`）`GetCurrentLeaderboard()`/`GetCurrentMemberLeaderboard()`原本只在收到response當下驗證一次時間範圍就快取結果，之後查詢永遠回傳同一份快取——改成儲存整個`DRT_MemberMatchResponse`，查詢當下才即時重新驗證時間範圍**+**`matchStatus==ONGOING`（新增`DRT_Const.MATCH_STATUS_ENDED`常數）。②（commit `2276b27b336`）再加一層`DRT_IMatchStateManager.HasOngoingMatch()`判斷，三個條件都要滿足才算資料有效。
5. **`DRT_MatchLeaderboardDropdownParser`三輪重構**（這個模塊這次動最多）：
   - 第一輪（commit `cae5a66cf78`）：今日賽事選項文字"MM/dd(進行中)"改成"MM/dd 進行中"（字表值`DAILY_RANK_TOURNAMENT_MATCH_DROPDOWN_ONGOING`從"(進行中)"拿掉括號，三語言一併處理），新增"配對中"字表Key(`DAILY_RANK_TOURNAMENT_MATCH_DROPDOWN_WAITING`)；「是否有今日賽事」判斷改成`currentLeaderboard.Count>0 || matchStatus==Waiting`（OR條件，讓配對中狀態也能顯示今日賽事選項，即使當時leaderboard必為空）
   - 第二輪（commit `77ee0c2f191`，**架構全面調整，使用者直接指名**）：「是否有今日賽事」完全改成只看`matchStatus`（Waiting或Ongoing即算有），**`matchRankingManager.GetCurrentLeaderboard()`從此完全不再被這個class呼叫**；同時把全部8個測試案例都追加驗證`option.status`欄位，多個測試function name跟著重新命名（例如`get_dropdown_options_when_no_current_match_and_no_history_then_return_empty_list`改語意但沒改名，其餘改用`match_status_xxx`命名慣例）。原本4個獨立的「status專門驗證」測試（None/Waiting/Ongoing/History各一個）因為每個內容測試都已經驗證status，全部刪除整合
   - **⚠️ `DRT_MatchRankingManager.GetCurrentLeaderboard()`目前只剩`DRT_MatchPageViewPresenter`還在用**，之後若那邊也不需要了，這個方法可能變成整條沒人呼叫，可以考慮一併檢視
6. **`DRT_SeasonStateManager`兩輪調整**：①（commit `d2be287cf6a`）`OnActivityStateInitComplete`原本忽略事件帶的`ActivityState`參數，一律呼叫`RequestSeasonLeaderboard()`——改成讀取該參數，`ActivityState.Close`時不執行任何動作，`Keep`/`Active`才照舊發請求（**⚠️這次換手敘述有筆誤來回確認兩次**：一開始寫成「若活動狀態為已結束→不執行」+「若活動狀態為保留中或**已結束**→發送請求」直接矛盾，使用者後來訂正第二句應為「保留中或**進行中**」才自洽，之後遇到同一個狀態值同時出現在「執行」跟「不執行」兩條案例裡，要主動提出可能是筆誤，不要照字面硬套）②（commit `0979e19099e`）`OnActivityStateInitComplete`跟`OnUpdateSeasonState`都新增`seasonSettings.IsSeasonRankingVisible()`守衛，不啟用時整個不執行任何動作（新增`DRT_ISeasonSettings`依賴，Setup預設mock為true，只有新測試案例覆寫成false）
7. **`DRT_SeasonRankingManager`移除截斷邏輯**（commit `e77a7166625`，使用者明確指示）：`GetSeasonLeaderboard()`原本會依`seasonSettings.GetDisplayRankCount()`把列表截斷到顯示上限，改成**直接回傳Server給的完整列表，不做任何截斷**——`GetDisplayRankCount()`本身沒有被廢棄，只是使用端換成`DRT_SeasonPageViewPresenter`（用來判斷名次要不要顯示成"XXX+"），跟這個模塊完全脫鉤；`DRT_ISeasonSettings`依賴因此從這個class整個移除（含`Initialize`簽名少一個參數）。截斷/超過上限相關的2個舊測試案例合併成1個「回傳完整列表」測試，架構文件用途概述跟依賴清單也同步更新（這段文字調整我是直接改寫後告知使用者，沒有事先問過確認新文字，跟`activity-iterate-module`原本的規範不完全一致，下次要更新用途概述前應該先問）

**⚠️ 工作區持續有兩筆使用者自己在IDE端進行中、未commit的異動，跟上述任何一次commit都無關，維持原樣不要清除**：
- `DRT_SeasonTimeManager.cs`：`OnReceiveEvent`收到`TournamentInfoResponse`時多了一行`//TODO: ForTest`＋`tournamentInfo.activityEndTime = tournamentInfo.lastMatchEndTime;`（暫時測試用假賦值，因為`activityEndTime`真正的Server欄位對應方式還沒確認）
- `DailyRankTournamentViewTester.cs`：疑似Tester相關的並行開發，內容未細看
- 另外`Assets/Scenes/PreLogin.unity`、多個材質/字型/Icon meta檔、`Assets/ThirdParty`submodule指標，都是Unity背景重新序列化或無關異動，每次commit前都有逐一排除，不用特別處理

**How to apply：** 新 session 若被要求「繼續處理分組日榜」，先跑`git status`/`git log -5`核對HEAD是否還是`55f5c8e053e`（不是的話代表使用者自己後續又有進度，要重新核實）。目前29個模塊只剩`DRT_FirstOpenTrigger`待處理，其餘都已經完成過至少一輪實作；但這次session的模式顯示**「已完成」不代表不會再被要求迭代**——使用者常會回頭對已完成的模塊追加行為調整（换手案例會直接列出新增/修改/移除的測試comment），這種請求走`activity-iterate-module`的日常維護模式即可，不用重新走完整的「拆解模塊」流程。commit前務必重新讀[[feedback_auto_commit_format]]全文，且每次都要完整看一次`git status`（不加pathspec），因為工作區經常混有使用者自己並行編輯的檔案，只能挑出跟當次任務直接相關的檔案`git add`，不能整批加入。

**2026-08-14追加：`DRT_MatchStateManager`更新類型判斷邏輯重構**（commit `55f5c8e053e`）：使用者事先直接手動改好`DRT_MatchStateUpdateType.cs`（新enum成員`UpdateToWaitingMatch`/`UpdateToOngoingMatch`/`UpdateSameMatch`取代舊的`UpdateSameDayMatch`/`UpdateNewDayMatch`，此為uncommitted狀態），這是這次任務的隱性前提，恢復工作/開始改動前若看到enum member跟production code對不上，要先確認是不是使用者自己已經手動改好新enum、等我方跟上實作，不要誤以為是編譯錯誤要回頭改enum。
- **判斷邏輯改為「lazy resolve」**：原本`successType`在MemberStatus API callback當下就算好（此時`memberMatchResponse`還是舊資料），改成用`Func<DRT_MatchStateUpdateType>`包裝，實際在MemberMatch API callback成功當下才真正呼叫`ResolveMatchStateUpdateType()`——這樣`GetCurrentMatchStatus()`才能讀到MemberMatch回應更新後的新鮮資料，不是用還沒刷新的舊`memberMatchResponse`比對。這是這次重構最關鍵的架構決策：**判斷「當前賽事狀態」的時機點必須挪到有機會讀到最新資料之後，不能沿用舊有「一開始就算好結果」的模式**，之後如果還有類似「新API結果出來後才能判斷分支」的需求，記得比照這個lazy resolve模式
- 判斷優先序：`matchId`與`GetCurrentMatchStatus()`皆與更新前相同 → `UpdateSameMatch`；否則依當前`GetCurrentMatchStatus()`分岔：`Waiting`→`UpdateToWaitingMatch`、`Ongoing`→`UpdateToOngoingMatch`、其餘(`None`，例如賽事已結束但`hasOngoingMatch`還沒更新的過渡態)→`NoUpdate`
- 換手案例裡凡是敘述「賽事ID無論有無改變」的都依使用者指示拆成matchId有改變/未改變兩條test case，共拆出6條（Waiting/Ongoing/None各2條）+ 原本1條`UpdateSameMatch`＝7條覆蓋這條判斷邏輯的全部分支
- **⚠️ 連帶壞掉的編譯依賴**：`DRT_MatchPageViewPresenter.OnMatchStateUpdateComplete()`原本判斷`type != UpdateNewDayMatch`才略過重抓歷史賽事清單，這個列舉值已被移除。**這條的正確條件使用者當下明確表示「後續會用新流程重寫測試，這次只要先讓它能編譯即可」**，故暫時改成`type != UpdateToWaitingMatch && type != UpdateToOngoingMatch`（只是讓既有測試意圖不變、能編譯過），`DRT_MatchPageViewPresenterTest.cs`裡7處`UpdateNewDayMatch`機械式取代為`UpdateToOngoingMatch`。**下次處理`DRT_MatchPageViewPresenter`時，這條件跟對應測試是使用者已知會整批重做的地方，不用意外**
- `DailyRankTournamentViewTester.cs`（Tester測試工具，非正式production邏輯）裡5處`UpdateSameDayMatch`依各自case本來就設定好的`dummyMatchStatus`(WAITING/ONGOING)機械式對應改成`UpdateToWaitingMatch`/`UpdateToOngoingMatch`
- 110個測試(`DRT_MatchStateManagerTest`+`DRT_MatchPageViewPresenterTest`)全過；順便跑了全DailyRankTournament 697個測試，7個失敗皆與本次改動無關（`DRT_SeasonTimeManagerTest`1個是使用者自己既有WIP假賦值造成、`DRT_ToastPopupManagerTest`6個跟`DRT_QualLevelUp` WS事件註冊有關且這次完全沒碰過那個檔案，判斷為既有問題不在本次任務範圍內，未處理）

**2026-08-13 同日session再追加：4個頁面ViewPresenter（資格/賽事/賽季/活動說明）行為修正**（commit `abe572adc07`）：這次是分支B（修改現有class），使用者已事先在4個test檔案裡各自寫好換手註解草稿，不需要走「等待換手」流程。
- `DRT_QualificationPageViewPresenter`/`DRT_MatchPageViewPresenter`/`DRT_SeasonPageViewPresenter`：`ClosePage()`原本無條件呼叫`view.Close()`（`DRT_MatchPageViewPresenter`還多呼叫`ForceCloseRankRewardTips()`/`ClearRankRewardTipsFollowTarget()`），若`view`已經因`UnbindView()`被設為null會直接NRE——三個都補上`if (view == null) return;`防呆（`isPageOpen = false`這行維持在guard之前，狀態該更新的部分不受影響）
- `DRT_ActivityInfoPageViewPresenter`：使用者發現這個class缺少其他3個頁面Presenter都有的「頁面顯示中」狀態旗標與對外查詢方法——新增`isPageOpen`欄位、介面新增`bool IsActivityInfoPageOpen()`，在`OnOpenHelpPage`/`OnOpenHelpMatchRewardPage`（此class沒有獨立的`ShowPage()`，這兩個事件handler本身就是它的「開啟頁面」入口）設為true，`ClosePage()`設為false；這個class原本`ClosePage()`已經有`view==null`防呆（跟前三者的缺失不同），純粹補齊查詢方法本身
- 202個測試(4個class全部)全過，`run_tests`+`read_console`確認無編譯錯誤後直接commit（依[[feedback_auto_run_test_and_compile]]慣例）
- 架構文件`DRT_ActivityInfoPageViewPresenter`小節Public Function清單已補上新方法

**2026-08-14追加：`DRT_MatchPageViewPresenter.OnMatchStateUpdateComplete()`拆分`UpdateToWaitingMatch`/`UpdateToOngoingMatch`兩條分支**（commit `1dfc4730c8a`）：原本兩種更新類型行為一致（都打`RequestApi`(MemberMatchInfo)、callback成功後執行完整`InitializePageContent()`），這次改成`UpdateToWaitingMatch`（賽事轉配對中）改成**不打API**，只呼叫`view.ShowMatchStatusText(ENDED文字)`顯示"賽事已結束"，不呼叫`ShowMatchCountDownTimer`（=隱藏倒數計時時間區塊，跟既有`RefreshCountDownSection`的History分支同一套"隱藏=不呼叫倒數計時方法"慣例，View interface沒有獨立的Hide方法）；`UpdateToOngoingMatch`維持原行為不變。使用者已在測試檔案先寫好換手草稿comment（只有一行敘述，沒補方法本體），這次補上對應`[Test]`方法。698個DailyRankTournament測試全過(697→698)，7個既有失敗(`DRT_SeasonTimeManagerTest`1個/`DRT_ToastPopupManagerTest`6個)跟這次改動無關，維持不動（詳見2026-08-14前一則記錄的原因）。
- 這次沒有動`InitializePageContent()`或其他區塊刷新方法，`UpdateToWaitingMatch`分支不會重新讀`matchLeaderboardDropdownParser.GetDropdownOptions()`，純粹是"收到配對中通知→顯示已結束狀態文字"這一個動作，跟原有"新一天賽事資料"那套完整刷新流程完全分開

**2026-08-14再追加：`DRT_MatchPageViewPresenter`刷新倒數計時區塊新增顯示/隱藏「倒數計時時間文字區塊」呼叫**（commit `32f2c47c64f`）：使用者自己先在`DRT_MatchPanel.cs`（View）新增`ShowMatchCountDownTimerText()`/`HideMatchCountDownTimerText()`兩個方法（控制`go_matchCountDownTimerText`這個GameObject顯示/隱藏，跟原本已存在、負責"設定倒數計時器數值並啟動"的`SetMatchCountDownTimer(long)`——這個方法本身也是使用者這次順便從`ShowMatchCountDownTimer`改名過來的——是分開的兩件事），但當下只加在concrete class沒有加進`DRT_IMatchPanel`介面，導致Presenter透過介面型別完全叫不到。這次補上：
- `DRT_IMatchPanel`介面新增這兩個方法宣告
- `RefreshCountDownSection()`：非Ongoing(History)分支顯示"賽事已結束"文字的同時呼叫`HideMatchCountDownTimerText()`；Ongoing分支真的算出倒數毫秒數並呼叫`SetMatchCountDownTimer()`後，緊接著呼叫`ShowMatchCountDownTimerText()`
- `OnMatchStateUpdateComplete()`的`UpdateToWaitingMatch`分支（上一則commit`1dfc4730c8a`新增的那條）同樣補上`HideMatchCountDownTimerText()`
- **判斷原則（使用者原話）**：換手案例/既有註解只要描述到「隱藏倒數計時時間區塊」的情境都要呼叫Hide；只有「有設定timer、有實際執行倒數計時流程」的情境才呼叫Show——`timeInfo==null`(Ongoing但拿不到賽事時間資訊)這個既有分支沒有被任何案例描述過"隱藏"，這次維持不動、不新增任何Show/Hide呼叫，不做超出範圍的假設
- 698個DailyRankTournament測試全過(數量不變，純粹在既有3個測試方法補斷言，沒有新增測試案例)
- **⚠️ Unity Editor這次又發生連續3次逾時/ping不通**（`get_test_job`連續timeout+disconnected），依循既有規則停止重試、請使用者確認Editor視窗狀態，使用者確認「已連上」後下一次呼叫立即恢復正常——沒有新的處理方式，純粹是既有模式的又一次應驗
- commit時額外發現`DRT_MainViewPresenterTest.cs`有一筆純粹刪除多餘空行的未commit異動(跟本次任何改動無關)，維持不動排除在外

**2026-08-14再追加：`DRT_SeasonPageViewPresenter`刷新主面板區塊新增判斷賽季排行榜資料是否為空**（commit `9bcd3d93706`）：原本`RefreshMainPanelSection()`只看賽季狀態(`NotStarted`→顯示未開始面板，`Ongoing`/`Ended`→一律顯示排行榜面板)，這次改成`Ongoing`/`Ended`狀態下**若`DRT_SeasonRankingManager.GetSeasonLeaderboard()`為空列表，也要顯示未開始面板**（跟NotStarted視覺上一致），只有真的有榜單資料才顯示排行榜面板。
- **關鍵重構**：`RefreshRankScrollSection()`原本自己呼叫一次`GetSeasonLeaderboard()`；這次`RefreshMainPanelSection`也需要讀同一份資料判斷是否為空，若各自呼叫會讓這個方法變成一輪刷新呼叫2次，導致既有兩個測試(`receive_switch_tab_event_when_season_tab_then_respond_to_subsequent_events`/`receive_season_state_update_complete_then_delay_and_refresh_page`)斷言的`Received(1).GetSeasonLeaderboard()`失敗——改成在`RefreshAllSections()`統一查一次、轉成參數傳給`RefreshMainPanelSection(leaderboard)`/`RefreshRankScrollSection(leaderboard)`共用，維持只呼叫1次。**這類「新增邏輯需要讀取某個既有查詢方法的回傳值，但該方法在別處已經被呼叫過且有次數斷言」的情境，優先考慮把查詢結果上提到共同呼叫者、用參數往下傳，而不是讓新分支自己再查一次**
- 使用者已在測試檔案先寫好兩則換手：一則只有註解(資料為空→顯示未開始面板)，一則是現有`[Test]`但內容還沒補上非空榜單資料(原本斷言"Ongoing/Ended就顯示排行榜面板"，沒考慮資料為空的情況)——這次補上前者的完整測試方法，並幫後者补上`GivenSeasonLeaderboard`非空資料，方法名稱同步改為`..._leaderboard_not_empty_then_show_season_ranking_panel`呼應新換手敘述
- 701個DailyRankTournament測試全過（跟698差3，但沒有深究確切原因，`DRT_SeasonPageViewPresenterTest`本身完全沒出現在失敗清單，判斷跟這次改動無關的既有落差，不影響本次驗證結論）；既有7個失敗維持不變
- **⚠️ Unity Editor這次又發生連續3次逾時/斷線**，一樣依既有規則請使用者確認視窗狀態後立即恢復，純粹又一次應驗既有模式
- commit時排除了三個跟這次任務無關、使用者自己並行編輯的檔案：`DailyRankTournamentViewTester.cs`、`DRT_ToastPopupQueueHandlerTest.cs`、`DRT_ToastPopupViewPresenterTest.cs`（皆為小量異動，疑似另一項通知彈窗相關的迭代草稿，未細看內容）

**How to apply（更新）：** 新 session 若被要求「繼續分組日榜」，`git log -5` 核對 HEAD 應為 `9bcd3d93706`（若不是代表使用者自己後續又有進度）。29個模塊仍是28完成、`DRT_FirstOpenTrigger`待處理不變——最近幾次都是對已完成模塊的行為細節迭代，不影響模塊拆解總進度。工作區目前應該還留有上述3個未commit的Toast/Tester並行異動，維持不動即可。
